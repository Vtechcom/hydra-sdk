---
topic: Các package mới hỗ trợ Hydra hoặc người dùng Hydra SDK
goal: Đề xuất package có giá trị dựa trên khoảng trống thực tế trong codebase và tài liệu, ưu tiên khả năng triển khai và tác động người dùng
mode: autonomous
status: complete
updated: 2026-08-11T11:07
---

- (technique) started Job to Be Done
- (idea by coach) @hydra-sdk/head-session: API workflow cấp cao để mở, nạp tiền, giao dịch, đóng và fanout một Head an toàn
- (idea by coach) @hydra-sdk/head-state: state machine xác định trạng thái hợp lệ và command hợp lệ tiếp theo
- (idea by coach) @hydra-sdk/receipts: biên nhận giao dịch L2 thống nhất từ TxValid đến SnapshotConfirmed
- (idea by coach) @hydra-sdk/evaluator: offline Plutus evaluator cung cấp IEvaluator cho TxBuilder
- (idea by coach) @hydra-sdk/providers: provider L1 độc lập khỏi core, bổ sung retry, cache và fallback
- (idea by coach) @hydra-sdk/cip30: dùng ví trình duyệt CIP-30 để ký deposit, decommit và giao dịch L1
- (idea by coach) @hydra-sdk/react: hooks/provider cho wallet, Head, snapshot và transaction status
- (idea by coach) @hydra-sdk/vue: composables/plugin cho wallet, Head, snapshot và transaction status
- (idea by coach) @hydra-sdk/testkit: mock connector, fixture protocol và assertion cho Hydra app
- (idea by coach) @hydra-sdk/devnet: khởi động local Hydra cluster có seed UTxO và lifecycle helpers
- (idea by coach) @hydra-sdk/observability: metrics, traces và health model cho bridge và tx pipeline
- (idea by coach) @hydra-sdk/indexer: lưu lịch sử Head, snapshot và giao dịch vào SQLite hoặc IndexedDB
- (idea by coach) @hydra-sdk/aiken-codegen: sinh codec TypeScript từ plutus.json blueprint
- (idea by coach) @hydra-sdk/keystore: encrypted wallet vault với adapter WebCrypto, filesystem và mobile secure storage
- (idea by coach) @hydra-sdk/router: chọn Head theo asset, participant, capacity, latency và availability
- (idea by coach) @hydra-sdk/payments: invoice, payment intent, refund và streaming payment trên Hydra
- (idea by coach) @hydra-sdk/multisig: phối hợp witness nhiều bên cho L1 deposit và giao dịch Hydra
- (idea by coach) @hydra-sdk/wallet-sync: đồng bộ số dư L1 và L2 thành một portfolio model
- (idea by coach) @hydra-sdk/compat: phát hiện hydra-node version và khóa API theo capability
- (idea by coach) @hydra-sdk/protocol-schema: runtime validation cho mọi payload và command Hydra
- (idea by coach) @hydra-sdk/simulator: mô phỏng Head và fault injection không cần node thật
- (idea by coach) @hydra-sdk/cli: scaffold app, quản lý keys, chạy Head và debug transaction
- (idea by coach) @hydra-sdk/policy: guardrail giao dịch theo hạn mức, asset allowlist và trạng thái Head
- (idea by coach) @hydra-sdk/funding-planner: lập kế hoạch deposit/decommit/fanout và tối ưu UTxO
- (idea by coach) @hydra-sdk/errors: taxonomy lỗi ổn định và error decoder cho node, provider, WASM
- (technique) started Build on What Works
- (idea by coach) Tách snapshot cache hiện có thành @hydra-sdk/head-store với storage adapter tùy chọn
- (idea by coach) Mở rộng awaitHydraMessage thành @hydra-sdk/workflows cho các saga có timeout và cleanup chuẩn
- (idea by coach) Biến HydraConnector thành hệ plugin connector cho direct node, Hexcore, proxy và mock
- (idea by coach) Đóng gói playground transaction builder thành component embeddable cho ứng dụng đối tác
- (idea by coach) Đóng gói infra/hydra-offline thành test harness npm có fixtures xác định
- (idea by coach) Biến tài liệu MCP thành @hydra-sdk/ai-tools để agent tạo code đúng phiên bản SDK
- (idea by coach) Biến converter benchmark thành @hydra-sdk/bench cho workload UTxO và snapshot
- (idea by coach) Chuyển WASM execution sang Web Worker package để tránh block UI
- (idea by coach) Tạo @hydra-sdk/tx-recipes trên TxBuilder cho transfer, mint, script spend và decommit phổ biến
- (idea by coach) Tạo @hydra-sdk/blueprint-codecs trên DatumUtils để encode/decode type-safe
- (idea by coach) Tạo @hydra-sdk/version-matrix từ tài liệu upstream để cảnh báo mismatch node/bridge
- (idea by coach) Tạo @hydra-sdk/head-query từ balance cache để cung cấp selector và reactive subscriptions
- (idea by coach) Tạo @hydra-sdk/provider-pool từ BaseWalletProvider để circuit-breaker nhiều backend
- (idea by coach) Tạo @hydra-sdk/hydra-provider để AppWallet dùng Head như một IFetcher/ISubmitter chuẩn
- (idea by coach) Tạo @hydra-sdk/tx-inspector từ deserializer helpers để explain CBOR trước khi ký
- (idea by coach) Tạo @hydra-sdk/tx-diff để so sánh draft, evaluated và signed transaction
- (idea by coach) Tạo @hydra-sdk/memory-safe từ dispose helpers để quản lý vòng đời object WASM tự động
- (idea by coach) Tạo @hydra-sdk/snapshot-stream để biến bridge events thành AsyncIterable hoặc Observable
- (idea by coach) Tạo @hydra-sdk/head-health từ syncedStatus, peer info, snapshot age và reconnect metrics
- (idea by coach) Tạo @hydra-sdk/deposit-manager từ commit/deposit endpoints và pending deposit types
- (idea by coach) Tạo @hydra-sdk/fanout-planner dựa trên partial fanout và finalized outputs
- (idea by coach) Tạo @hydra-sdk/migration-codemod để chuyển bridge v1 sang v2
- (idea by coach) Tạo @hydra-sdk/docs-components từ playground presets và code generation UI
- (idea by coach) Tạo @hydra-sdk/provider-fixtures để contract-test Blockfrost, Ogmios và Demeter
- (idea by coach) Tạo @hydra-sdk/release-compat để test package exports trên Vite, Nuxt, Next và Node
- (technique) started Assumption Reversal
- (idea by coach) Nếu app không giữ kết nối lâu dài: @hydra-sdk/head-client stateless qua request/receipt
- (idea by coach) Nếu người dùng không sở hữu mnemonic: @hydra-sdk/external-signers cho CIP-30, KMS và hardware wallet
- (idea by coach) Nếu một app dùng nhiều Head: @hydra-sdk/router với discovery và failover
- (idea by coach) Nếu không có node thật khi phát triển: @hydra-sdk/simulator chạy hoàn toàn in-process
- (idea by coach) Nếu snapshot không đủ cho UI: @hydra-sdk/indexer lưu event history và derived views
- (idea by coach) Nếu developer không hiểu UTxO: @hydra-sdk/intent chuyển payment intent thành transaction plan
- (idea by coach) Nếu transaction không nên được ký mù: @hydra-sdk/tx-explain diễn giải human-readable
- (idea by coach) Nếu provider trả lỗi không được phép giả thành empty array: @hydra-sdk/provider-contract với typed errors
- (idea by coach) Nếu protocol types không nên viết tay: @hydra-sdk/protocol-codegen sinh từ upstream schema/source
- (idea by coach) Nếu version mismatch là trạng thái bình thường: @hydra-sdk/capabilities negotiation thay vì semver guessing
- (idea by coach) Nếu HydraBridge không nên chứa cache: @hydra-sdk/head-store nhận event và quản lý projections
- (idea by coach) Nếu framework UI không nên import WASM: @hydra-sdk/client facade tải lazy và chạy worker
- (idea by coach) Nếu mỗi dApp không nên tự retry: @hydra-sdk/resilience với backoff, idempotency và circuit breaker
- (idea by coach) Nếu lifecycle không nên điều khiển bằng callback: @hydra-sdk/workflow dùng async state machine
- (idea by coach) Nếu đóng Head không phải lựa chọn nhị phân: @hydra-sdk/exit-planner chọn decommit, partial fanout hoặc close
- (idea by coach) Nếu asset cần đi qua nhiều Head: @hydra-sdk/liquidity-router lập lộ trình nạp/rút
- (idea by coach) Nếu giao dịch L1 và L2 là một luồng: @hydra-sdk/settlement orchestrate deposit-use-withdraw
- (idea by coach) Nếu observability là API người dùng: @hydra-sdk/diagnostics trả health report có remediation
- (idea by coach) Nếu test phải phá hệ thống: @hydra-sdk/chaos giả lập disconnect, rollback, stale snapshot và unsynced node
- (idea by coach) Nếu package phải chạy mobile: @hydra-sdk/mobile cung cấp RN WASM loader và secure key adapter
- (idea by coach) Nếu docs không đủ: @hydra-sdk/examples-cli tải recipe theo version đang cài
- (idea by coach) Nếu người dùng không muốn quản lý protocol params: @hydra-sdk/network-config resolve và cache params
- (idea by coach) Nếu developer chỉ muốn nhận tiền: @hydra-sdk/checkout cung cấp invoice và confirmation abstraction
- (idea by coach) Nếu application cần quyền hạn: @hydra-sdk/session-keys quản lý scoped keys và spending limits
- (idea by coach) Nếu Head là backend realtime chung: @hydra-sdk/realtime-state cung cấp replicated state primitives
- (technique) started Time Horizon Ladder
- (idea by coach) 3 tháng: @hydra-sdk/testkit từ fixtures bridge và infra offline hiện có
- (idea by coach) 3 tháng: @hydra-sdk/evaluator bọc UPLC dependency đã có trong core
- (idea by coach) 3 tháng: @hydra-sdk/cip30 với signer/fetcher adapters tối thiểu
- (idea by coach) 3 tháng: @hydra-sdk/tx-inspector cho CBOR, fee, assets, scripts và witnesses
- (idea by coach) 3 tháng: @hydra-sdk/compat đọc Greetings và capability-map theo node version
- (idea by coach) 6 tháng: @hydra-sdk/head-session gom happy path deposit-to-payment-to-decommit
- (idea by coach) 6 tháng: @hydra-sdk/observability tích hợp OpenTelemetry và Prometheus exporters
- (idea by coach) 6 tháng: @hydra-sdk/provider-pool có retry, cache, typed errors và fallback
- (idea by coach) 6 tháng: @hydra-sdk/aiken-codegen sinh datum/redeemer codecs từ blueprint
- (idea by coach) 6 tháng: @hydra-sdk/vue và @hydra-sdk/react trên head-store chung
- (idea by coach) 6 tháng: @hydra-sdk/worker cô lập WASM và transaction building khỏi main thread
- (idea by coach) 12 tháng: @hydra-sdk/indexer đa runtime SQLite/IndexedDB
- (idea by coach) 12 tháng: @hydra-sdk/router chọn Head theo health, balance và policy
- (idea by coach) 12 tháng: @hydra-sdk/devnet điều phối multi-party local cluster trong CI
- (idea by coach) 12 tháng: @hydra-sdk/migration-codemod theo các major hydra-node
- (idea by coach) 18 tháng: @hydra-sdk/payments chuẩn hóa invoice, refund và streaming payments
- (idea by coach) 18 tháng: @hydra-sdk/session-keys cho game và delegated spending
- (idea by coach) 18 tháng: @hydra-sdk/liquidity cung cấp inventory và rebalancing giữa nhiều Head
- (idea by coach) 2 năm: @hydra-sdk/settlement bảo đảm workflow L1-L2 có recovery log
- (idea by coach) 2 năm: @hydra-sdk/fleet điều hành nhiều Hydra Head như một pool dịch vụ
- (idea by coach) 3 năm: @hydra-sdk/intent compiler dịch yêu cầu nghiệp vụ thành transaction/workflow
- (idea by coach) 3 năm: @hydra-sdk/verified-protocol dùng generated codecs và property tests upstream
- (idea by coach) 5 năm: @hydra-sdk/head-market discovery Head công khai theo asset và SLA
- (idea by coach) 5 năm: @hydra-sdk/autopilot tự tối ưu Head capacity, fanout và routing
- (idea by coach) 10 năm: @hydra-sdk/unified-ledger che giấu ranh giới L1/L2 khỏi ứng dụng
- (decision) Impact-Effort shortlist: 1 testkit (cao/thấp-vừa), 2 head-session (rất cao/vừa), 3 evaluator (cao/vừa-cao), 4 cip30 (cao/vừa), 5 observability (cao/vừa), 6 router (tiềm năng rất cao/cao, để sau)
- (decision) NUF ranking: testkit 26/30, head-session 25/30, evaluator 24/30, cip30 23/30, observability 22/30, router 20/30; provider-pool và protocol-codegen nên là phase hoặc package hỗ trợ
- (insight) Through-line: SDK đã mạnh ở primitive (wallet, tx, bridge, WASM) nhưng thiếu lớp productization gồm workflow cấp cao, môi trường test tái sử dụng, evaluation tự chứa và adapters tới ví/UI; routing là hướng chiến lược khi hệ sinh thái multi-head trưởng thành

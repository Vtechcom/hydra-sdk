---
idea: @hydra-sdk/evaluator - offline Plutus transaction evaluator cho IEvaluator của TxBuilder
goal: Khóa product boundary, execution model, context contract, compatibility claims, API surface và phạm vi V1 trước khi implement
status: complete
updated: 2026-08-11T14:47
---

- (assumption) Người dùng chính là DApp developer và maintainer TxBuilder cần exUnits thật trong local dev, CI, browser hoặc private runtime mà không cần API key hay node/provider bên ngoài
- (crack) @harmoniclabs/uplc hiện có trong core chỉ parse, encode và apply parameters; nó không phải CEK machine hoặc transaction evaluator hoàn chỉnh, nên không được quảng bá package mới như một wrapper mỏng của dependency sẵn có
- (direction) Strongest shape là transaction-level offline evaluator: decode transaction và witnesses, resolve ledger inputs/scripts/datums, build ScriptContext theo era, execute từng redeemer bằng engine có cost accounting, rồi trả EvalAction đúng IEvaluator
- (lock) @hydra-sdk/evaluator implement IEvaluator của @hydra-sdk/transaction; tích hợp mặc định là createEvaluator(...), sau đó truyền thẳng vào TxBuilder
- (lock) Offline nghĩa là evaluation không thực hiện network I/O; mọi UTxO, datum, reference script, protocol parameters, cost models, era và slot/time context phải đến từ transaction, additionalUtxos/additionalTxs hoặc context source do caller cung cấp
- (crack) TxBuilder hiện gọi evaluateTx(txHex) mà không truyền resolved inputs; evaluator không thể tạo đúng ScriptContext cho spending scripts nếu transaction chỉ chứa input references
- (lock) V1 yêu cầu một EvaluatorContextSource cục bộ hoặc preloaded; missing input, datum, reference script, cost model hay era context là typed error, tuyệt đối không fallback sang zero, placeholder hoặc network fetch ngầm
- (lock) Package không sở hữu provider HTTP. Blockfrost, Ogmios và Demeter adapters thuộc provider layer; evaluator có thể dùng chúng làm oracle trong conformance tests nhưng không gọi chúng trong offline execution
- (direction) Public surface tối thiểu gồm createEvaluator, OfflineEvaluator, EvaluationContext/EvaluatorContextSource, EvaluatorError và evaluateTxDetailed; evaluateTx giữ đúng IEvaluator và chỉ trả EvalAction
- (lock) V1 hỗ trợ Plutus V1, V2 và V3 với SPEND và MINT end-to-end, đồng bộ với khả năng remap hiện tại của TxBuilder
- (lock) CERT, REWARD, VOTE và PROPOSE bị loại khỏi V1 cho đến khi cả evaluator lẫn TxBuilder có transaction-context và remapping conformance hoàn chỉnh; không trả budget mà caller không thể áp dụng an toàn
- (crack) Kết quả deterministic chỉ đúng khi toàn bộ ledger context, cost models, era/slot mapping và engine version được pin; cùng txHex nhưng context khác có thể cho budget hoặc failure khác
- (lock) Claim sản phẩm là deterministic offline evaluation với pinned context, không phải cardano-node-equivalent mặc định
- (lock) Mọi release phải chạy differential conformance corpus với một node/Ogmios version được công bố; compatibility matrix ghi engine, Plutus language, protocol era, cost model hash và sai lệch budget cho phép
- (direction) evaluateTxDetailed trả diagnostics có redeemer pointer, script hash/language, budget, logs, context fingerprint và failure phase; logs bị giới hạn kích thước và tắt mặc định để phù hợp browser/CI
- (lock) Runtime target là Node và browser/worker; core evaluator không dùng filesystem, process hoặc network, và không import @emurgo/cardano-serialization-lib trực tiếp
- (lock) Package dùng @hydra-sdk/cardano-wasm cho Cardano CBOR object graph; engine UPLC/ledger được cô lập sau adapter để có thể thay thế mà không đổi IEvaluator
- (lock) V1 loại trừ transaction submission, fee balancing, script compilation, debugger từng bước, remote provider fallback, Hydra ledger validation và tuyên bố production acceptance thay cho cardano-node
- (decision) Delivery order: feasibility spike một real SPEND V3 fixture và một MINT fixture, context resolver, transaction/redeemer mapping, Plutus V1-V3 engine adapter, detailed diagnostics, rồi differential conformance matrix
- (lock) Go/no-go gate: budgets và failures phải khớp oracle trên corpus đại diện; nếu engine không hỗ trợ semantics/cost accounting cần thiết thì dừng package hoặc đổi engine trước khi công bố API stable
- (event) Idea hardened; tạo forged-idea.md và forge-report.html; implementation được để cho workflow tiếp theo

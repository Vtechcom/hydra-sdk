---
idea: @hydra-sdk/mpc - kit dựng MPC wallet nhanh cho DApps để ký giao dịch dùng UTxO trong Hydra Head mà CIP-30/CIP-8 extension từ chối vì UTxO không tồn tại trên L1
goal: Đánh giá product fit, tính khả thi mật mã/Cardano, ranh giới package và MVP đáng triển khai
status: complete
updated: 2026-08-11T12:10
---

- (assumption) Foreign UTxO nghĩa là UTxO thuộc payment credential của người dùng nhưng chỉ tồn tại trong Hydra snapshot, không phải input thực sự thuộc người khác
- (crack) CIP-8 là message signing, không phải transaction signing; điểm chặn trực tiếp là CIP-30 signTx policy. CIP-8 có thể dùng cho enrollment/session authorization
- (crack) Nếu mục tiêu duy nhất là bypass extension policy thì MPC là over-engineering; embedded/session key wallet giải đơn giản hơn
- (direction) Strongest framing: Hydra Session MPC Wallet - CIP-30 wallet dùng cho L1 funding/identity, aggregate MPC key giữ và ký tài sản trong Hydra Head
- (crack) Package-only không đủ: production cần coordinator service, durable nonce/session state, policy engine, recovery và security audit
- (direction) Go/no-go spike: DKG plain Ed25519 key, derive Cardano enterprise address, seed offline Head, threshold-sign tx body hash, assemble CSL vkey witness, submit và nhận SnapshotConfirmed
- (note) Pivotal unresolved decision: MPC nhằm tạo non-custodial/recoverable/policy-controlled signer hay chỉ thay thế extension bị chặn
- (lock) Mục tiêu sản phẩm là signer non-custodial, seedless, cho phép DApp onboarding bằng Social OAuth Google Apple GitHub
- (crack) OAuth là authentication chứ không phải secret/key ownership; nếu OAuth alone khôi phục toàn bộ signing power thì mô hình trở thành custodial hoặc social-custodial
- (direction) Routine signing nên dùng device share + coordinator share; OAuth chỉ authorize coordinator share, device share được bảo vệ bằng passkey/WebAuthn hoặc secure storage
- (direction) Recovery production nên hướng 2-of-3 và resharing: device, coordinator, recovery factor độc lập; không reconstruct full private key
- (crack) Identity phải bind bằng OIDC issuer+subject, không dùng email; linking provider mới cần wallet signature hoặc recovery proof
- (note) Unresolved product boundary: wallet riêng theo từng DApp hay một identity/wallet portable dùng xuyên nhiều DApp
- (note) Per-DApp model: mỗi DApp tạo aggregate key/address riêng cho user, OAuth client và coordinator scoped theo DApp, blast radius nhỏ và SDK-first nhưng tài sản/identity phân mảnh
- (note) Portable model: một wallet identity dùng xuyên DApp, cần neutral coordinator/auth broker và cross-origin signing surface; UX tốt hơn nhưng biến dự án thành wallet platform với blast radius và vận hành lớn
- (direction) Recommended sequencing: ship per-DApp MPC wallet first, thiết kế protocol để sau này thêm portable account hub; không cố threshold-HD derivation trong MVP
- (lock) Mỗi wallet chỉ thuộc một DApp; cùng user đăng nhập DApp khác sẽ tạo MPC wallet và Cardano address độc lập
- (direction) Wallet identity key nên bind bằng tenantId + network + OIDC issuer + subject; tuyệt đối không dùng email làm định danh
- (direction) Per-DApp isolation cho phép policy riêng, coordinator self-host hoặc managed, và compromise một DApp không lan sang wallet của DApp khác
- (crack) Seedless onboarding chưa giải seedless recovery; nếu OAuth alone khôi phục được wallet thì recovery authority có thể trở thành custodial
- (lock) Recovery bắt buộc đồng thời Social OAuth và passkey; không hỗ trợ OAuth-only recovery
- (direction) OAuth locates account via tenantId+iss+sub; passkey is origin-bound possession proof and must bind recovery/signing session challenge
- (crack) WebAuthn assertion alone không tạo encryption key để unwrap share; cần validate PRF extension/hmac-secret support hoặc dùng recovery share độc lập
- (direction) Preferred recovery flow: OAuth+passkey authorizes secure share unwrap or threshold resharing to new device; old device is revoked, full private key never reconstructed
- (crack) Nếu coordinator và recovery authority cùng một operator có thể collude để sign thì non-custodial claim phải bị hạ cấp; trust domains cần tách hoặc passkey-derived secret phải giữ share ngoài server
- (note) Self-host coordinator: DApp giữ server share và vận hành OAuth verification, nonce state, policy, DB/HSM; giảm phụ thuộc vendor nhưng tăng DevOps/security burden và rủi ro DApp shutdown
- (note) Managed coordinator: Hydra SDK vận hành multi-tenant signing service; onboarding nhanh và security đồng nhất nhưng tạo central availability/vendor dependency và cross-tenant blast radius
- (direction) Cả hai có thể cryptographically non-custodial nếu coordinator share không thể sign alone; phải hỗ trợ coordinator migration/resharing để tránh operational lock-in
- (direction) Recommended product architecture: một open protocol và cùng một coordinator implementation self-hostable; managed service chỉ là deployment chính thức của cùng code
- (lock) Coordinator delivery model là hybrid: managed service mặc định và cùng implementation có thể self-host
- (lock) Managed và self-host phải dùng cùng open protocol, storage schema compatibility và conformance suite
- (direction) Coordinator endpoint là adapter; DApp có thể đổi managed sang self-host bằng threshold resharing/participant rotation mà aggregate public key và wallet address không đổi
- (crack) Nếu migration yêu cầu coordinator cũ hợp tác thì sudden provider failure vẫn khóa tài sản; production topology cần recovery path độc lập
- (note) Next decision: spike 2-of-2 đơn giản trước hay triển khai production topology 2-of-3 ngay từ v1
- (lock) Không cần Cardano/FROST compatibility spike: hydraone-web-client đã derive aggregate Ed25519 address, sign tx body hash, build VKey witness, merge signed CBOR và submit Hydra transactions
- (lock) Production topology bắt buộc 2-of-3 ngay từ đầu
- (note) Reference 2-of-3 hiện tại: Web3Auth social share + browser device share + password recovery share; login needs social plus device or recovery
- (direction) MPC package thay password recovery factor bằng passkey-backed recovery factor và không cho wallet hoạt động production trước khi hashed 1-of-1 factor bị xóa
- (crack) Reference dùng Web3Auth managed network; nó chưa chứng minh hybrid self-host/managed, coordinator portability hoặc resharing giữa deployments
- (direction) Implementation work shifts to extracting reusable Cardano signer, passkey factor lifecycle, open coordinator protocol, tenant isolation, migration and conformance tests
- (lock) Web3Auth chỉ là optional compatibility adapter/reference, không phải dependency bắt buộc hoặc nền tảng coordinator của package
- (lock) Final 2-of-3 topology: A device share, B coordinator share, C passkey-protected recovery share; routine A+B, recovery B+C, coordinator rotation A+C
- (lock) Package scope: @hydra-sdk/mpc client/Cardano signer, @hydra-sdk/mpc-coordinator open hybrid service, @hydra-sdk/mpc-web3auth optional adapter
- (lock) V1 excludes global portable wallet, HD/BIP32 derivation, OAuth-only recovery, password recovery and production 1-of-1/2-of-2 modes
- (lock) Reference hydraone implementation eliminates Cardano witness compatibility spike; delivery begins with extraction, passkey factor replacement, coordinator abstraction and conformance tests

---
idea: @hydra-sdk/testkit - bộ công cụ test dùng lại được cho Hydra SDK và DApps, từ mock deterministic đến hydra-node offline thật
goal: Khóa product boundary, API surface, binary policy, isolation model và phạm vi V1 trước khi implement sau
status: complete
updated: 2026-08-11T12:20
---

- (assumption) Người dùng chính là maintainer Hydra SDK và DApp developer cần test bridge, transaction flow và failure handling mà không tự dựng mocks hoặc copy shell infra
- (crack) Một mock-only package sẽ lặp lại sai wire format; live hydra-node 2.3.0 từng bắt ba lỗi mà unit mocks không thấy
- (direction) Strongest shape là testkit hai tầng: deterministic mock/replay cho unit test và real offline hydra-node controller cho contract/E2E
- (lock) @hydra-sdk/testkit là dev-only package; không nằm trong production runtime của DApp
- (lock) Testkit không triển khai lại Hydra protocol hoặc ledger; mocks chỉ replay kịch bản và validate command, real node là source of truth
- (lock) Public surface chia subpath: @hydra-sdk/testkit/fixtures, /mock, /offline và /vitest
- (lock) Mock tier gồm MockHydraConnector, typed scenario builder, command assertions, deterministic event replay và controllable clock
- (lock) Fault injection V1 gồm disconnect/reconnect, delay/timeout, malformed hoặc untagged payload, rejected command và snapshot events đến lệch thứ tự
- (lock) Fixtures được capture/version theo hydra-node release, typed bằng bridge payloads và kiểm chứng bằng real-node conformance tests
- (lock) Offline tier cung cấp HydraTestHead.start/stop, dynamic ports, temp credentials/persistence, seeded UTxO và cleanup idempotent
- (crack) Shared Head hiện buộc Vitest tắt parallelism vì snapshot và UTxO race; package chỉ đáng giá nếu mỗi instance/worker được isolate
- (lock) Parallel safety là invariant: mỗi Head có port set, head seed, persistence và credentials riêng; stop có thể gọi nhiều lần và luôn reap child process
- (lock) Không bundle binary hydra-node khoảng 428 MB và không tự tải qua network; ưu tiên HYDRA_NODE_BIN hoặc explicit resolver/downloader có checksum và cache
- (direction) Vitest adapter quản lý lifecycle, timeout, diagnostics và artifact dump khi fail nhưng core offline controller không phụ thuộc Vitest
- (lock) Test wallet/seed/UTxO builders phải deterministic, được đánh dấu test-only và không bao giờ được trình bày như cấu hình production an toàn
- (crack) Offline mode không có Cardano L1 và luôn InSync nên không thể chứng minh deposit, decommit, close, contest, fanout hoặc unsynced behavior
- (lock) V1 loại trừ L1 devnet orchestration, multi-node consensus, load/performance harness và general-purpose protocol simulator
- (lock) Compatibility matrix gắn testkit fixtures với hydra-node version và align major compatibility với @hydra-sdk/bridge
- (decision) MVP extraction order: fixtures and MockHydraConnector, isolated HydraTestHead controller, Vitest adapter, sau đó fault catalogue và conformance matrix
- (lock) Artifact lần này chỉ khóa ý tưởng và contract bề mặt; implementation package để sau
- (event) Idea hardened; forged-idea.md và self-contained forge-report.html đã tạo; implementation được hoãn theo yêu cầu người dùng

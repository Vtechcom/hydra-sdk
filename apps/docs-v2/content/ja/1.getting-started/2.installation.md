---
title: インストール
description: プロジェクトに Hydra SDK パッケージをインストールする方法を学びます
navigation:
  icon: i-lucide-download
---

::warning
**`@hydra-sdk/bridge` v2 は `next` タグで公開されています。** v2 は `hydra-node` **2.x** を対象とする破壊的変更です。`latest` タグは引き続き **1.3.2**（`hydra-node` **1.x** 対応）を指します。

v2 ノードを使用する場合は明示的にインストールしてください:

```bash
pnpm add @hydra-sdk/bridge@next
```

アップグレード前に[マイグレーションガイド](/resources/migration)を参照してください。
::

::tip
**AI アシスタントを使用していますか？** リポジトリの [`INSTALL.md`](https://github.com/Vtechcom/hydra-sdk/blob/master/INSTALL.md) を参照するとルールベースのセットアップガイドが得られます。または docs の [MCP server](/ai) を接続すると、これらのページを直接クエリできます。
::

Hydra SDK は個別のパッケージとして NPM 上で公開されており、プロジェクトに必要なものだけをインストールできます。

## AI アシスタントでインストール

次のプロンプトを AI コーディングアシスタント（Claude、Cursor、Copilot など）に貼り付けて、Hydra SDK をインストール・設定してください。機械可読なドキュメントを指し示し、つまずきやすいルールを明示します：

```text
Install and configure Hydra SDK (@hydra-sdk) in this project.

Read these for the exact steps:
- https://hydrasdk.com/raw/getting-started/installation.md
- https://hydrasdk.com/raw/getting-started/configuration.md

Rules (follow strictly):
1. Always install @hydra-sdk/cardano-wasm together with the other @hydra-sdk/* packages.
2. Never import @emurgo/cardano-serialization-lib directly — use @hydra-sdk/cardano-wasm.
3. NETWORK_ID is a Record<Network, number> with keys MAINNET | PREPROD | PREVIEW (no TESTNET), not an enum.
4. For a browser build with Vite, add vite-plugin-wasm + vite-plugin-top-level-await + a buffer polyfill, and exclude @hydra-sdk/cardano-wasm from optimizeDeps.

Then create an AppWallet on PREPROD and log its base address to verify the setup.
```

## システム要件

SDK をインストールする前に、システムが次の要件を満たしていることを確認してください。

- **Node.js**: >= 18.20.0
- **npm**: >= 8.0.0（または **pnpm** >= 8.0.0、**yarn** >= 1.22.0）
- **TypeScript**: >= 5.0.0（任意ですが推奨）

## コアパッケージ

### 個別パッケージのインストール

必要なパッケージをインストールしてください。ただし、常に `@hydra-sdk/cardano-wasm` を含めてください。

```bash
# Core wallet functionality
npm install @hydra-sdk/core @hydra-sdk/cardano-wasm

# Hydra Layer 2 integration
npm install @hydra-sdk/bridge @hydra-sdk/cardano-wasm

# Transaction building utilities
npm install @hydra-sdk/transaction @hydra-sdk/cardano-wasm
```

::note
`@hydra-sdk/cardano-wasm` は、他のすべてのパッケージがインポート経路として通る WASM コアです。推移的に取り込まれる場合でも、**直接の依存関係**としてインストールしてください。そうすることで、バンドラーが設定でき（`vite-plugin-wasm`、`optimizeDeps` からの除外）、pnpm の厳格な `node_modules` でも解決できます。バンドラーの設定については[設定](/getting-started/configuration)を参照してください。
::

### 一括バンドルのインストール

ほとんどのプロジェクトでは、コアパッケージをまとめてインストールします。

```bash
# Install all essential packages
npm install @hydra-sdk/core @hydra-sdk/bridge @hydra-sdk/transaction @hydra-sdk/cardano-wasm
```

## 次のステップ

SDK のインストールが完了したので、次に進みましょう。

- [クイックスタートガイド](/getting-started/quick-start) - 最初のウォレットを作成する
- [設定ガイド](/getting-started/configuration) - ビルドツールをセットアップする
- **[ウォレットアプリを構築する](/guides/build-wallet-app)** - 完全に動作する React & Vue アプリ
- [API リファレンス](/api) - すべての API を探索する

---
title: Tích hợp từ Cardano-CLI
description: Tìm hiểu cách dùng khóa được tạo bởi Cardano-CLI với Hydra SDK bằng CardanoCliWallet.
navigation:
  icon: i-lucide-key-round
---

Tìm hiểu cách dùng khóa được tạo từ Cardano-CLI với Hydra SDK. Lớp `CardanoCliWallet` cho phép bạn dùng các khóa CLI hiện có trong ứng dụng Node.js của mình để ký giao dịch theo chương trình và tương tác với blockchain.

**Tham khảo**: [Tài liệu Cardano-CLI](https://developers.cardano.org/docs/get-started/cardano-cli/basic-operations/get-started)

## Tạo khóa với Cardano-CLI

Tạo một cặp khóa thanh toán bằng Cardano-CLI:

```bash
cardano-cli address key-gen \
  --verification-key-file payment.vkey \
  --signing-key-file payment.skey
```

Việc này sẽ tạo ra hai tệp:

**`payment.vkey`** (Verification Key):
```json
{
    "type": "PaymentVerificationKeyShelley_ed25519",
    "description": "Payment Verification Key",
    "cborHex": "5820832ba166c8ba8afda5b9d85dfe13dd8fffd460da79a2c3cf34e107216637985b"
}
```

**`payment.skey`** (Signing Key):
```json
{
    "type": "PaymentSigningKeyShelley_ed25519",
    "description": "Payment Signing Key",
    "cborHex": "5820bd09ad4f98cd103e059ab62d17a6a7d920b16d9f0eed3eb6b77d3ca8f61dc117"
}
```

::warning
⚠️ **Cảnh báo bảo mật**: Không bao giờ chia sẻ signing key (`payment.skey`) của bạn hoặc commit nó vào version control.
::

## Xác minh khóa (tùy chọn)

Xác minh khóa của bạn bằng cách xây dựng một địa chỉ:

```bash
cardano-cli address build \
  --payment-verification-key-file payment.vkey \
  --testnet-magic 1 \
  --out-file payment.addr
```

**Kết quả** (`payment.addr`):
```
addr_test1vz5hhyn6ecl66a2ca3cwfnwu8ddnp24hakfq2k37rhk28ysk8g0wz
```

::note
Đây là một **enterprise address** (chỉ thanh toán, không có staking key) tuân theo [CIP-19](https://cips.cardano.org/cips/cip19/).
::

## Dùng với Hydra SDK

### Thiết lập cơ bản

```ts
import { CardanoCliWallet, NETWORK_ID } from '@hydra-sdk/core';

// Extract the cborHex values from your key files
const skey = '5820bd09ad4f98cd103e059ab62d17a6a7d920b16d9f0eed3eb6b77d3ca8f61dc117';
const vkey = '5820832ba166c8ba8afda5b9d85dfe13dd8fffd460da79a2c3cf34e107216637985b';

// Initialize the wallet
const wallet = new CardanoCliWallet({ 
    skey, 
    vkey, 
    networkId: NETWORK_ID.PREPROD 
});

// Get address in Bech32 format
const address = wallet.getAddressBech32();
console.log('Wallet Address:', address);
// Output: addr_test1vz5hhyn6ecl66a2ca3cwfnwu8ddnp24hakfq2k37rhk28ysk8g0wz
```

### Ví dụ giao dịch

```ts
import { 
    CardanoCliWallet, 
    NETWORK_ID,
    ProviderUtils
} from '@hydra-sdk/core';
import { TxBuilder } from '@hydra-sdk/transaction';

// Initialize wallet with CLI keys
const wallet = new CardanoCliWallet({ 
    skey: '5820bd09ad4f98cd103e059ab62d17a6a7d920b16d9f0eed3eb6b77d3ca8f61dc117',
    vkey: '5820832ba166c8ba8afda5b9d85dfe13dd8fffd460da79a2c3cf34e107216637985b',
    networkId: NETWORK_ID.PREPROD 
});

// Setup provider
const provider = new ProviderUtils.BlockfrostProvider({
    apiKey: 'your-blockfrost-project-id',
    network: 'preprod'
});

// Build transaction
const txBuilder = new TxBuilder({
    fetcher: provider.fetcher,
    submitter: provider.submitter
});
const unsignedTx = await txBuilder
    // ...
    .complete();

// Sign transaction
const signedTxCbor = await wallet.signTx(unsignedTx.to_hex());
console.log('Signed Transaction:', signedTxCbor);

```

## Tài liệu tham khảo API

### Constructor

```ts
new CardanoCliWallet(options: CardanoCliWalletConfig)
```

**Các tùy chọn**:
- `skey` (bắt buộc): CBOR hex của signing key (định dạng: `5820...`)
- `vkey` (bắt buộc): CBOR hex của verification key (định dạng: `5820...`)
- `networkId` (tùy chọn): `NETWORK_ID.MAINNET` (1) hoặc `NETWORK_ID.PREPROD` (0), mặc định là MAINNET
- `fetcher` (tùy chọn): Fetcher tùy chỉnh để truy vấn UTxO
- `submitter` (tùy chọn): Submitter tùy chỉnh để gửi giao dịch

### Các phương thức

```ts
// Get address in Bech32 format
getAddressBech32(): string

// Sign a transaction
signTx(unsignedTxHex: string, partialSign?: boolean): Promise<string>

// Get network ID
getNetworkId(): number

// Submit transaction (requires submitter in constructor)
submitTx(tx: string): Promise<string>

// Query UTxOs (requires fetcher in constructor)
queryUTxOs(address: string): Promise<UTxO[]>
```

### Các thuộc tính

```ts
// Get payment signing key
paymentSKey: CardanoWASM.PrivateKey

// Get payment verification key
paymentVKey: CardanoWASM.PublicKey
```

## Bước tiếp theo

- Tìm hiểu về [Xây dựng giao dịch](/guides/transactions)
- Khám phá [Làm việc với utilities](/guides/utilities-cookbook)
- Xem thêm [Ví dụ](/guides)

## Tài nguyên liên quan

- [Tài liệu Cardano-CLI](https://developers.cardano.org/docs/get-started/cardano-cli/basic-operations/get-started)
- [CIP-19: Địa chỉ Cardano](https://cips.cardano.org/cips/cip19/)
- [Hydra SDK Core API](/api/core)

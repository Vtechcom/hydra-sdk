---
title: '@hydra-sdk/core - Utilities'
description: Cardanoブロックチェーンデータ、WASM操作、一般的な開発タスクのための包括的なユーティリティ関数コレクション
---

# @hydra-sdk/core – Utilities

> Hydra SDKは、Cardanoブロックチェーンデータ、WASM操作、一般的な開発タスクに作業するための包括的なユーティリティ関数コレクションを提供します。

## 概要

ユーティリティは複数のカテゴリに整理されています：

- **[Keys Utilities](#keys-utilities)** - 暗号キーの作成と管理
- **[Data Parsing](#data-parsing)** - データ形式間の変換
- **[Datum Utilities](#datum-utilities)** - Plutusデータ構造の作業
- **[Redeemer Utilities](#redeemer-utilities)** - Plutus スクリプトの redeemer を構築
- **[Validation Utilities](#validation-utilities)** - アドレスとトランザクション出力の検証
- **[Policy Utilities](#policy-utilities)** - ミントポリシーの管理
- **[Metadata Utilities](#metadata-utilities)** - トランザクションメタデータの変換
- **[Time Utilities](#time-utilities)** - スロットと時間の計算
- **[Provider Utilities](#provider-utilities)** - ブロックチェーンデータプロバイダーの抽象化
- **[Cardano WASM Utilities](#cardano-wasm-utilities)** - 低レベルブロックチェーン操作

---

## インポート

すべてのユーティリティは、より良い整理のために名前空間付きモジュールとしてエクスポートされます：

```typescript
import {
	Serializer,
	Deserializer,
	Resolver,
	Converter,
	BuildKeys,
	ParserUtils,
	TimeUtils,
	DatumUtils,
	RedeemerUtils,
	ValidationUtils,
	PolicyUtils,
	MetadataUtils,
	ProviderUtils,
	CostModels,
	KeysUtils,
	SLOT_CONFIG_NETWORK
} from '@hydra-sdk/core'
```

---

## Keys Utilities :badge[1.1.5+]{variant="success" style="transform: translateY(-2px)"}

### **`KeysUtils.cardanoCliKeygen() → { sk: CardanoCLiSkey; vk: CardanoCLiVkey }`**

Cardano CLIと互換性のあるed25519キーペアを生成します。

説明: Cardano CLI形式と互換性のある署名キーおよび検証キーを生成します。

**例:**

```typescript
import { KeysUtils } from '@hydra-sdk/core'

const cardanoKeys = KeysUtils.cardanoCliKeygen()
console.log({
	skey: cardanoKeys.sk.cborHex, // PaymentSigningKeyShelley_ed25519
	vkey: cardanoKeys.vk.cborHex // PaymentVerificationKeyShelley_ed25519
})
```

**戻り値:** `{ sk: CardanoCLiSkey; vk: CardanoCLiVkey }`

```typescript
{
  sk: CardanoCLiSkey,
  vk: CardanoCLiVkey,
}
```

---

### **`KeysUtils.hydraCliKeygen() → { sk: HydraCliSkey; vk: HydraCliVkey }`**

Hydraと互換性のあるed25519キーペアを生成します。

説明: Hydra CLI形式と互換性のある署名キーおよび検証キーを生成します。

**例:**

```typescript
import { KeysUtils } from '@hydra-sdk/core'

const hydraKeys = KeysUtils.hydraCliKeygen()
console.log({
	skey: hydraKeys.sk.cborHex, // HydraSigningKey_ed25519
	vkey: hydraKeys.vk.cborHex // HydraVerificationKey_ed25519
})
```

**戻り値:** `{ sk: HydraCliSkey; vk: HydraCliVkey }`

```typescript
{
  sk: HydraCliSkey,
  vk: HydraCliVkey,
}
```

---

### **`KeysUtils.genVkey(skey: CardanoCLiSkey | HydraCliSkey) → CardanoCLiVkey | HydraCliVkey`**

署名キーから検証キーを生成します。

**パラメータ:**

- `skey` (CardanoCLiSkey | HydraCliSkey): ソース署名キー

**例:**

```typescript
import { KeysUtils } from '@hydra-sdk/core'

const skey = KeysUtils.cardanoCliKeygen().sk
const vkey = KeysUtils.genVkey(skey)
console.log('Verification Key:', vkey.cborHex)
```

**戻り値:** `CardanoCLiVkey | HydraCliVkey`
対応する検証キー

---

### **`KeysUtils.mnemonicToCliKey(mnemonic: string[], accountIndex?: number, keyIndex?: number)`**

**→ `{ sk: CardanoCLiSkey; vk: CardanoCLiVkey }`** :badge[1.1.6+]{variant="success"}

BIP-39ニーモニックフレーズをCardano CLI互換のキーペアに変換します。

説明: BIP-44派生パス（m/1852'/1815'/account'/0/keyIndex）を使用してニーモニックフレーズから支払いキーを派生します。これにより、CardanoウォレットやCLIツールと互換性のあるキーを生成できます。

**パラメータ:**

- `mnemonic` (string[]): BIP-39ニーモニックワード（12または24ワード）
- `accountIndex` (number, optional): 派生パスのアカウントインデックス（デフォルト: 0）
- `keyIndex` (number, optional): 派生パスのキーインデックス（デフォルト: 0）

**例:**

```typescript
import { KeysUtils, AppWallet } from '@hydra-sdk/core'

// ランダムなニーモニックを生成
const mnemonic = AppWallet.brew()

// デフォルトのインデックスでCLIキーに変換
const keys = KeysUtils.mnemonicToCliKey(mnemonic)
console.log({
	skey: keys.sk.cborHex,
	vkey: keys.vk.cborHex
})

// 特定のアカウントインデックスを使用（複数アカウント用）
const account1Keys = KeysUtils.mnemonicToCliKey(mnemonic, 1)

// 特定のキーインデックスを使用（複数アドレス用）
const addressKeys = KeysUtils.mnemonicToCliKey(mnemonic, 0, 5)
```

**戻り値:** `{ sk: CardanoCLiSkey; vk: CardanoCLiVkey }`

```typescript
{
  sk: CardanoCLiSkey,  // PaymentSigningKeyShelley_ed25519
  vk: CardanoCLiVkey   // PaymentVerificationKeyShelley_ed25519
}
```

**重要な注意事項:**

- BIP-44派生パスを使用: `m/1852'/1815'/accountIndex'/0/keyIndex`
- 異なる`accountIndex`値は、同じニーモニックから異なるアカウントを作成します
- 異なる`keyIndex`値は、同じアカウントの異なる支払いアドレスを作成します
- キーは決定論的 - 同じニーモニック + インデックスは常に同じキーを生成します
- Cardano CLIおよびほとんどのCardanoウォレットと互換性があります

---

## Data Parsing

### **`ParserUtils.bytesToHex(bytes: ArrayBuffer | Uint8Array | Buffer) → string`**

バイトをhex文字列に変換します。

**パラメータ:**

- `bytes` (ArrayBuffer | Uint8Array | Buffer): 変換するバイトデータ

**例:**

```typescript
import { ParserUtils } from '@hydra-sdk/core'

const bytes = new Uint8Array([72, 101, 108, 108, 111])
const hex = ParserUtils.bytesToHex(bytes)
console.log(hex) // "48656c6c6f"
```

**戻り値:** `string`
Hex文字列

---

### **`ParserUtils.hexToBytes(hex: string) → Buffer`**

hex文字列をバイトに変換します。

**パラメータ:**

- `hex` (string): 変換するHex文字列

**例:**

```typescript
import { ParserUtils } from '@hydra-sdk/core'

const bytes = ParserUtils.hexToBytes('48656c6c6f')
console.log(bytes) // <Buffer 48 65 6c 6c 6f>
```

**戻り値:** `Buffer`
バイトデータ

---

### **`ParserUtils.stringToHex(str: string) → string`**

UTF-8文字列をhexに変換します。

**パラメータ:**

- `str` (string): UTF-8文字列

**例:**

```typescript
import { ParserUtils } from '@hydra-sdk/core'

const hex = ParserUtils.stringToHex('Hello World')
console.log(hex) // "48656c6c6f20576f726c64"
```

**戻り値:** `string`
Hex文字列

---

### **`ParserUtils.hexToString(hex: string) → string`**

hex文字列をUTF-8に変換します。

**パラメータ:**

- `hex` (string): 変換するHex文字列

**例:**

```typescript
import { ParserUtils } from '@hydra-sdk/core'

const str = ParserUtils.hexToString('48656c6c6f20576f726c64')
console.log(str) // "Hello World"
```

**戻り値:** `string`
UTF-8文字列

---

### **`ParserUtils.toBytes(strOrHex: string) → Uint8Array`**

文字列またはhexをバイトに変換します。

**パラメータ:**

- `strOrHex` (string): 文字列またはhex文字列

**例:**

```typescript
import { ParserUtils } from '@hydra-sdk/core'

const bytes1 = ParserUtils.toBytes('Hello World')
const bytes2 = ParserUtils.toBytes('48656c6c6f')
console.log(bytes1, bytes2)
```

**戻り値:** `Uint8Array`
バイトデータ

---

### **`ParserUtils.fromUTF8(str: string) → string`**

UTF-8文字列をhexに変換します（エイリアス）。

**パラメータ:**

- `str` (string): UTF-8文字列

**例:**

```typescript
import { ParserUtils } from '@hydra-sdk/core'

const hex = ParserUtils.fromUTF8('Hello')
console.log(hex) // "48656c6c6f"
```

**戻り値:** `string`
Hex文字列

---

### **`ParserUtils.toUTF8(hex: string) → string`**

hex文字列をUTF-8に変換します（エイリアス）。

**パラメータ:**

- `hex` (string): Hex文字列

**例:**

```typescript
import { ParserUtils } from '@hydra-sdk/core'

const str = ParserUtils.toUTF8('48656c6c6f')
console.log(str) // "Hello"
```

**戻り値:** `string`
UTF-8文字列

---

## Datum Utilities

### **`DatumUtils.mkInt(n: string | number | bigint) → PlutusData`**

整数datumを作成します。

**パラメータ:**

- `n` (string | number | bigint): 整数値

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const intDatum = DatumUtils.mkInt(42)
const bigIntDatum = DatumUtils.mkInt(9007199254740991n)
console.log(intDatum.to_hex())
```

**戻り値:** `PlutusData`
整数datum

---

### **`DatumUtils.mkBytes(hex: string) → PlutusData`**

hex文字列からバイトdatumを作成します。

**パラメータ:**

- `hex` (string): バイトデータのHex文字列

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const bytesDatum = DatumUtils.mkBytes('deadbeef')
console.log(bytesDatum.to_hex())
```

**戻り値:** `PlutusData`
バイトdatum

---

### **`DatumUtils.mkConstr(alt: number, fields: PlutusData[]) → PlutusData`**

コンストラクタdatumを作成します。

**パラメータ:**

- `alt` (number): コンストラクタインデックス（0ベース）
- `fields` (PlutusData[]): Plutusデータフィールドの配列

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const constrDatum = DatumUtils.mkConstr(0, [
	DatumUtils.mkInt(42),
	DatumUtils.mkBytes('deadbeef')
])
console.log(constrDatum.to_hex())
```

**戻り値:** `PlutusData`
コンストラクタdatum

---

### **`DatumUtils.mkMap(entries: Array<[PlutusData, PlutusMapValues]>) → PlutusData`**

マップdatumを作成します。

**パラメータ:**

- `entries` (Array<[PlutusData, PlutusMapValues]>): キーと値のペアの配列

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const mapDatum = DatumUtils.mkMap([
	[DatumUtils.mkInt(1), DatumUtils.mkBytes('value1')],
	[DatumUtils.mkInt(2), DatumUtils.mkBytes('value2')]
])
console.log(mapDatum.to_hex())
```

**戻り値:** `PlutusData`
マップdatum

---

### **`DatumUtils.mkList(elements: PlutusData[]) → PlutusData`** <Badge type="tip" text="New in v1.3.2" />

Plutus データ要素の配列からリスト datum を作成します。

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const listDatum = DatumUtils.mkList([DatumUtils.mkInt(1), DatumUtils.mkInt(2)])
console.log(listDatum.to_hex())
```

**戻り値:** `PlutusData`

---

### **`DatumUtils.mkBool(value: boolean) → PlutusData`** <Badge type="tip" text="New in v1.4.0" />

真偽値 datum を作成します。`false` は `Constr(0, [])`、`true` は `Constr(1, [])` としてエンコードされます。

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const trueDatum = DatumUtils.mkBool(true)
console.log(trueDatum.to_hex())
```

**戻り値:** `PlutusData`

---

### **`DatumUtils.mkOption(value?: PlutusData | null) → PlutusData`** <Badge type="tip" text="New in v1.4.0" />

オプション datum を作成します。`Some` は `Constr(0, [value])`、`None` (値なし/`null`) は `Constr(1, [])` です。

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const some = DatumUtils.mkOption(DatumUtils.mkInt(42))
const none = DatumUtils.mkOption()
```

**戻り値:** `PlutusData`

---

### **`DatumUtils.mkBytesList(items: Array<string | Uint8Array>) → PlutusData`** <Badge type="tip" text="New in v1.4.0" />

バイト文字列 (hex) または `Uint8Array` 値の配列からリスト datum を作成します。

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const bytesList = DatumUtils.mkBytesList(['deadbeef', new Uint8Array([1, 2, 3])])
```

**戻り値:** `PlutusData`

---

### **`DatumUtils.mkIntList(items: Array<string | number | bigint>) → PlutusData`** <Badge type="tip" text="New in v1.4.0" />

整数値の配列からリスト datum を作成します。

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const intList = DatumUtils.mkIntList([1, 2, 9007199254740991n])
```

**戻り値:** `PlutusData`

---

### **`DatumUtils.mkOutputRef(ref: { txHash: string; index: number | bigint }) → PlutusData`** <Badge type="tip" text="New in v1.4.0" />

出力参照 datum を作成します。`Constr(0, [Bytes(txHash), Int(index)])` としてエンコードされます。

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const outputRef = DatumUtils.mkOutputRef({
	txHash: '1d2e5b97f1cad7bea2b06144abc4974012fa786c9e7e5faddd03243b967a03f6',
	index: 0
})
```

**戻り値:** `PlutusData`

---

### **`DatumUtils.mkAddress(bech32: string) → PlutusData`** <Badge type="tip" text="New in v1.4.0" />

bech32 Cardano アドレスを Plutus データ表現にエンコードします。

**例:**

```typescript
import { DatumUtils } from '@hydra-sdk/core'

const addrDatum = DatumUtils.mkAddress('addr1qx2fxv2umyhttkxyxp8x0...')
```

**戻り値:** `PlutusData`

---

### **`DatumUtils.parseAddress(data: PlutusData, networkId?: number) → string`** <Badge type="tip" text="New in v1.4.0" />

Plutus アドレスデータを bech32 アドレス文字列にデコードします。

**パラメータ:**

- `data` (PlutusData): Plutus アドレスデータ
- `networkId` (number, optional): 生成されるアドレスのネットワーク ID (デフォルト: MAINNET)

**例:**

```typescript
import { DatumUtils, NETWORK_ID } from '@hydra-sdk/core'

const bech32 = DatumUtils.parseAddress(addrDatum, NETWORK_ID.PREPROD)
```

**戻り値:** `string`
Bech32 アドレス

---

## Redeemer Utilities :badge[1.4.0+]{variant="success" style="transform: translateY(-2px)"}

Plutus スクリプトの redeemer を構築します。Import: `import { RedeemerUtils } from '@hydra-sdk/core'`。

### **`RedeemerUtils.mkRedeemer(data: PlutusData, options?: BuildRedeemerOptions) → Redeemer`**

Plutus データを `Redeemer` にラップします。

**パラメータ:**

- `data` (PlutusData): Redeemer データ
- `options` (BuildRedeemerOptions, optional):
  - `tag` ('spend' | 'mint' | 'reward' | 'cert' | 'vote' | 'voting_proposal'、大文字小文字を区別しません): Redeemer タグ。デフォルト: `'spend'`
  - `index` (string | number | bigint): Redeemer インデックス。デフォルト: `0`
  - `exUnits` (`{ mem, steps }`): 実行ユニット予算。デフォルト: `RedeemerUtils.DEFAULT_EX_UNITS`

**例:**

```typescript
import { RedeemerUtils, DatumUtils } from '@hydra-sdk/core'

const data = DatumUtils.mkConstr(1, [])
const redeemer = RedeemerUtils.mkRedeemer(data, { tag: 'spend', index: 0 })
```

**戻り値:** `Redeemer`

---

### **`RedeemerUtils.mkSpendRedeemer(data: PlutusData, options?) → Redeemer`**

タグが `spend` にプリセットされた redeemer を構築します。

**例:**

```typescript
import { RedeemerUtils, DatumUtils } from '@hydra-sdk/core'

const redeemer = RedeemerUtils.mkSpendRedeemer(DatumUtils.mkConstr(0, []))
```

**戻り値:** `Redeemer`

---

### **`RedeemerUtils.mkMintRedeemer(data: PlutusData, options?) → Redeemer`**

タグが `mint` にプリセットされた redeemer を構築します。

**例:**

```typescript
import { RedeemerUtils, DatumUtils } from '@hydra-sdk/core'

const redeemer = RedeemerUtils.mkMintRedeemer(DatumUtils.mkConstr(0, []))
```

**戻り値:** `Redeemer`

---

### **`RedeemerUtils.mkUnitRedeemer(options?) → Redeemer`**

Unit redeemer `Constr(0, [])` — 「引数なし」の redeemer を構築します。

**例:**

```typescript
import { RedeemerUtils } from '@hydra-sdk/core'

const redeemer = RedeemerUtils.mkUnitRedeemer()
```

**戻り値:** `Redeemer`

---

### **`RedeemerUtils.mkExUnits({ mem, steps }) → ExUnits`**

`ExUnits` 実行予算を構築します。`RedeemerUtils.DEFAULT_EX_UNITS` は `{ mem: '5000000', steps: '2000000000' }` です (プレースホルダーの予算 — 本番環境ではスクリプトを評価してください)。

**例:**

```typescript
import { RedeemerUtils } from '@hydra-sdk/core'

const exUnits = RedeemerUtils.mkExUnits({ mem: '5000000', steps: '2000000000' })
```

**戻り値:** `ExUnits`

---

## Validation Utilities :badge[1.4.0+]{variant="success" style="transform: translateY(-2px)"}

### **`ValidationUtils.isValidTxOutput(output: TxOutput) → boolean`**

トランザクション出力が有効かどうかをチェックします。Import: `import { ValidationUtils } from '@hydra-sdk/core'`。

**パラメータ:**

- `output` (TxOutput): 検証するトランザクション出力

**例:**

```typescript
import { ValidationUtils } from '@hydra-sdk/core'

const isValid = ValidationUtils.isValidTxOutput(txOutput)
console.log('Output valid:', isValid)
```

**戻り値:** `boolean`

---

## Policy Utilities

### **`PolicyUtils.buildPolicyScriptFromPubkey(pubkeyScript: PubkeyScript) → string`**

公開鍵からポリシースクリプトを構築します。

**パラメータ:**

- `pubkeyScript` (PubkeyScript):
  - `type`: 'sig'
  - `keyHash` (string): 公開鍵ハッシュ

**例:**

```typescript
import { PolicyUtils } from '@hydra-sdk/core'

const policyScript = PolicyUtils.buildPolicyScriptFromPubkey({
	type: 'sig',
	keyHash: 'abc123def456...'
})
console.log('Policy Script:', policyScript)
```

**戻り値:** `string`
JSON形式のネイティブスクリプト

---

### **`PolicyUtils.buildMintingPolicyScriptFromAddress(addressBech32: string) → string`**

アドレスからミントポリシースクリプトを構築します。

**パラメータ:**

- `addressBech32` (string): Cardanoアドレス（bech32）

**例:**

```typescript
import { PolicyUtils } from '@hydra-sdk/core'

const mintingScript = PolicyUtils.buildMintingPolicyScriptFromAddress(
	'addr1qx2fxv2umyhttkxyxp8x0dlpdt3k6cwng5pxj3jhsydzer3jcu5d8ps7zex2k2xt3uqxgjqnnj83ws8lhrn648jjxtwq2ytjqp'
)
console.log('Minting Script:', mintingScript)
```

**戻り値:** `string`
JSON形式のスクリプト

---

### **`PolicyUtils.buildMintingPolicyScriptFromKeyHash(keyHash: string) → string`**

キーハッシュからミントポリシースクリプトを構築します。

**パラメータ:**

- `keyHash` (string): 公開鍵ハッシュ（hex）

**例:**

```typescript
import { PolicyUtils } from '@hydra-sdk/core'

const mintingScript =
	PolicyUtils.buildMintingPolicyScriptFromKeyHash('abc123...')
console.log('Minting Script:', mintingScript)
```

**戻り値:** `string`
JSON形式のスクリプト

---

### **`PolicyUtils.policyIdFromNativeScript(nativeScript: string) → string`**

ネイティブスクリプトからPolicy IDを抽出します。

**パラメータ:**

- `nativeScript` (string): JSON文字列形式のネイティブスクリプト

**例:**

```typescript
import { PolicyUtils } from '@hydra-sdk/core'

const policyId = PolicyUtils.policyIdFromNativeScript(policyScript)
console.log('Policy ID:', policyId)
```

**戻り値:** `string`
Policy ID（hexハッシュ）

---

## Metadata Utilities

### **`MetadataUtils.metadataObjToMetadatum(metadata: any) → TransactionMetadatum`**

さまざまなデータ型をCardanoトランザクションメタデータに変換します。

**パラメータ:**

- `metadata` (any): 変換するデータ（string, number, bigint, Uint8Array, array, object, Map）

**例:**

```typescript
import { MetadataUtils } from '@hydra-sdk/core'

// 文字列を変換
const stringMetadata = MetadataUtils.metadataObjToMetadatum('Hello World')

// 数値を変換
const numberMetadata = MetadataUtils.metadataObjToMetadatum(42)

// bigintを変換
const bigintMetadata = MetadataUtils.metadataObjToMetadatum(9007199254740991n)

// バイトを変換
const bytesMetadata = MetadataUtils.metadataObjToMetadatum(
	new Uint8Array([1, 2, 3, 4])
)

// 配列を変換
const arrayMetadata = MetadataUtils.metadataObjToMetadatum([
	'item1',
	42,
	new Uint8Array([5, 6])
])

// オブジェクトをマップに変換
const objectMetadata = MetadataUtils.metadataObjToMetadatum({
	name: 'NFT Name',
	image: 'ipfs://...',
	attributes: ['trait1', 'trait2']
})

// Mapを変換
const mapMetadata = MetadataUtils.metadataObjToMetadatum(
	new Map([
		['key1', 'value1'],
		['key2', 42]
	])
)
```

**戻り値:** `TransactionMetadatum`
Cardano形式のメタデータ

**重要な注意事項:**

- 最大メタデータ `bytes` サイズ: **64 bytes**
- 最大メタデータ `text` サイズ: **64 characters**
- 配列とMapは再帰的に処理されます。メタデータ全体のサイズに注意してください

---

## Time Utilities

### **`TimeUtils.slotToBeginUnixTime(slot: number, slotConfig: SlotConfig) → number`**

スロットをUnixタイムスタンプに変換します。

**パラメータ:**

- `slot` (number): スロット番号
- `slotConfig` (SlotConfig): スロット設定（PREPROD、MAINNETなど）

**例:**

```typescript
import { TimeUtils, SLOT_CONFIG_NETWORK } from '@hydra-sdk/core'

const timestamp = TimeUtils.slotToBeginUnixTime(
	1000,
	SLOT_CONFIG_NETWORK.PREPROD
)
const date = new Date(timestamp * 1000)
console.log('スロット開始時刻:', date.toLocaleString())
```

**戻り値:** `number`
Unixタイムスタンプ（秒）

---

### **`TimeUtils.unixTimeToEnclosingSlot(unixTime: number, slotConfig: SlotConfig) → number`**

Unixタイムスタンプをスロットに変換します。

**パラメータ:**

- `unixTime` (number): Unixタイムスタンプ（秒）
- `slotConfig` (SlotConfig): スロット設定

**例:**

```typescript
import { TimeUtils, SLOT_CONFIG_NETWORK } from '@hydra-sdk/core'

const slot = TimeUtils.unixTimeToEnclosingSlot(
	Math.floor(Date.now() / 1000),
	SLOT_CONFIG_NETWORK.PREPROD
)
console.log('現在のスロット:', slot)
```

**戻り値:** `number`
スロット番号

---

### **`TimeUtils.resolveSlotNo(network: Network, milliseconds?: number) → string`**

指定した時刻におけるネットワークのスロット番号を決定します。

**パラメータ:**

- `network` (Network): 対象ネットワーク（`'MAINNET'` | `'PREPROD'` | `'PREVIEW'`）
- `milliseconds` (number, optional): ミリ秒単位のタイムスタンプ（デフォルト: `Date.now()`）

**例:**

```typescript
import { TimeUtils } from '@hydra-sdk/core'

const slotNo = TimeUtils.resolveSlotNo('PREPROD')
console.log('現在のスロット:', slotNo)
```

**戻り値:** `string`
文字列としてのスロット番号

---

### **`TimeUtils.resolveEpochNo(network: Network, milliseconds?: number) → number`**

指定した時刻におけるネットワークのエポック番号を決定します。

**パラメータ:**

- `network` (Network): 対象ネットワーク（`'MAINNET'` | `'PREPROD'` | `'PREVIEW'`）
- `milliseconds` (number, optional): ミリ秒単位のタイムスタンプ（デフォルト: `Date.now()`）

**例:**

```typescript
import { TimeUtils } from '@hydra-sdk/core'

const epochNo = TimeUtils.resolveEpochNo('PREPROD')
console.log('現在のエポック:', epochNo)
```

**戻り値:** `number`
エポック番号

---

### **`TimeUtils.buildHydraSlotConfig(startTimestamp: number, options?: object) → SlotConfig`**

Head の開始タイムスタンプを基準にした Hydra スロット設定を構築します。

**パラメータ:**

- `startTimestamp` (number): Head の開始タイムスタンプ（ミリ秒、必須）
- `options` (object, optional): `zeroSlot`、`slotLength`、`startEpoch`、`epochLength` のオーバーライド

**例:**

```typescript
import { TimeUtils } from '@hydra-sdk/core'

const slotConfig = TimeUtils.buildHydraSlotConfig(Date.now())
console.log('Hydra Slot Config:', {
	zeroTime: slotConfig.zeroTime,
	zeroSlot: slotConfig.zeroSlot,
	slotLength: slotConfig.slotLength
})
```

**戻り値:** `SlotConfig`
スロット設定

---

## Provider Utilities

### **`ProviderUtils.BlockfrostProvider(config: BlockfrostProviderConfig) → BlockfrostProvider`**

Cardanoブロックチェーンにクエリとトランザクション送信を行うためのBlockfrostプロバイダーを作成します。

説明: BlockfrostProviderは、UTxOの取得とトランザクション送信のためにBlockfrost APIに接続するウォレットプロバイダーです。BaseWalletProviderを拡張し、fetcherとsubmitter機能の両方を提供します。

**パラメータ:**

- `config` (BlockfrostProviderConfig):
  - `apiKey` (string): [Blockfrost Dashboard](https://blockfrost.io/)からのBlockfrost Project ID
  - `network` (BlockfrostSupportedNetworks): 接続するネットワーク（'mainnet'、'preprod'、または'preview'）
  - `apiVersion` (number, optional): Blockfrost APIバージョン（デフォルト: 0）
  - `baseURL` (string, optional): デフォルトの Blockfrost エンドポイント URL を上書きします。Demeter などの Blockfrost 互換 API に便利です。 <Badge type="tip" text="New in v1.3.2" />
  - `cachingOptions` (object, optional): キャッシング設定
    - `enabled` (boolean): レスポンスキャッシングを有効化（デフォルト: false）
    - `maxSize` (number): 最大キャッシュアイテム数（デフォルト: 100）
    - `ttl` (number): ミリ秒単位の生存時間（デフォルト: 300000 - 5分）

**プロパティ:**

- `fetcher` (IFetcher): ブロックチェーンデータを取得するためのインターフェース
  - `fetchAddressUTxOs(address: string, asset?: string): Promise<UTxO[]>` - アドレスのUTxOを取得、オプションでアセットでフィルター
- `submitter` (ISubmitter): トランザクション送信のためのインターフェース
  - `submitTx(txHex: string): Promise<string>` - 署名済みトランザクションを送信し、トランザクションハッシュを返す

**例:**

```typescript
import { ProviderUtils } from '@hydra-sdk/core'

// 基本設定
const provider = new ProviderUtils.BlockfrostProvider({
	apiKey: 'preprodXXXXXXXXXXXXXXXXXXXX',
	network: 'preprod'
})

// APIバージョンとキャッシング付き
const providerWithCache = new ProviderUtils.BlockfrostProvider({
	apiKey: 'preprodXXXXXXXXXXXXXXXXXXXX',
	network: 'preprod',
	apiVersion: 0,
	cachingOptions: {
		enabled: true,
		maxSize: 200,
		ttl: 600000 // 10分
	}
})

// アドレスのすべてのUTxOを取得
const utxos = await provider.fetcher.fetchAddressUTxOs(
	'addr1qx2fxv2umyhttkxyxp8x0dlpdt3k6cwng5pxj3jhsydzer...'
)

// 特定のアセットを含むUTxOを取得
const assetUtxos = await provider.fetcher.fetchAddressUTxOs(
	'addr1qx2fxv2umyhttkxyxp8x0dlpdt3k6cwng5pxj3jhsydzer...',
	'lovelace' // またはpolicy_id + asset_name
)

// 署名済みトランザクションを送信
const txHash = await provider.submitter.submitTx(signedTxHex)
console.log('トランザクション送信:', txHash)
```

**戻り値:** `BlockfrostProvider`
プロバイダーインスタンス

---

### **`ProviderUtils.OgmiosProvider(config: OgmiosProviderConfig) → OgmiosProvider`**

JSON-RPC 2.0プロトコルを使用してクエリとトランザクション送信を行うOgmiosプロバイダーを作成します。

説明: OgmiosProviderは、UTxOの取得とトランザクション送信のためにHTTP経由でOgmios APIに接続するウォレットプロバイダーです。BaseWalletProviderを拡張し、JSON-RPC 2.0プロトコルを使用してOgmiosサーバーと通信します。

**パラメータ:**

- `config` (OgmiosProviderConfig):
  - `network` (OgmiosSupportedNetworks): 接続するネットワーク（'mainnet'、'preprod'、または'preview'）
  - `apiEndpoint` (string, optional): Ogmios APIエンドポイントURL

**プロパティ:**

- `fetcher` (IFetcher): ブロックチェーンデータを取得するためのインターフェース
  - `fetchAddressUTxOs(address: string, asset?: string): Promise<UTxO[]>` - アドレスのUTxOを取得、オプションでアセットでフィルター
- `submitter` (ISubmitter): トランザクション送信のためのインターフェース
  - `submitTx(tx: string): Promise<string>` - 署名済みトランザクションを送信し、トランザクションハッシュを返す

**例:**

```typescript
import { ProviderUtils } from '@hydra-sdk/core'

// リモートOgmiosインスタンスに接続
const remoteProvider = new ProviderUtils.OgmiosProvider({
	network: 'preprod',
	apiEndpoint: 'https://preprod.ogmios.cardano-rpc.hydrawallet.app'
})

// アドレスのすべてのUTxOを取得
const utxos = await remoteProvider.fetcher.fetchAddressUTxOs(
	'addr1qx2fxv2umyhttkxyxp8x0dlpdt3k6cwng5pxj3jhsydzer...'
)

// 特定のアセットを含むUTxOを取得
const assetUtxos = await remoteProvider.fetcher.fetchAddressUTxOs(
	'addr1qx2fxv2umyhttkxyxp8x0dlpdt3k6cwng5pxj3jhsydzer...',
	'lovelace' // またはpolicy_id + asset_name
)

// 署名済みトランザクションを送信
const txHash = await remoteProvider.submitter.submitTx(signedTxHex)
console.log('トランザクション送信:', txHash)
```

**戻り値:** `OgmiosProvider`
fetcherとsubmitterインターフェースを持つプロバイダーインスタンス

---

### **`ProviderUtils.DemeterProvider(config: DemeterProviderConfig) → DemeterProvider`** <Badge type="tip" text="New in v1.3.2" />

[Demeter](https://demeter.run) の Blockfrost 互換ホスティングエンドポイント向けのプロバイダーを作成します。`BlockfrostProvider` を継承しているため、同じ `fetcher`/`submitter` インターフェースを公開します。認証は `authToken` を通じてエンドポイントのサブドメインに埋め込まれます。

**パラメータ:**

- `config` (DemeterProviderConfig):
  - `authToken` (string): Demeter の認証トークン（認証済みサブドメインとしても使用されます）
  - `network` (DemeterSupportedNetworks): 接続するネットワーク（'mainnet'、'preprod'、または'preview'）
  - `apiVersion` (number, optional): Blockfrost APIバージョン（デフォルト: 0）
  - `cachingOptions` (object, optional): `BlockfrostProvider` と同じキャッシング設定

**例:**

```typescript
import { ProviderUtils } from '@hydra-sdk/core'

const provider = new ProviderUtils.DemeterProvider({
	authToken: 'blockfrost102lx3ckhzvkjjh7677g',
	network: 'preprod'
})

// BlockfrostProvider と同じ fetcher / submitter インターフェース
const utxos = await provider.fetcher.fetchAddressUTxOs(
	'addr1qx2fxv2umyhttkxyxp8x0dlpdt3k6cwng5pxj3jhsydzer...'
)
const txHash = await provider.submitter.submitTx(signedTxHex)
```

エンドポイントは `https://{authToken}.cardano-{network}.blockfrost-m1.demeter.run/api/v{apiVersion ?? 0}` として構築されます。

**戻り値:** `DemeterProvider`
fetcherとsubmitterインターフェースを持つプロバイダーインスタンス

---

## Cardano WASM Utilities

### **`Serializer.serializeAssetUnit(policyId: string, assetName: string) → string`**

ポリシー ID とアセット名（どちらも hex）を連結してアセットユニット文字列を構築します。

**パラメータ:**

- `policyId` (string): ポリシー ID（hex）
- `assetName` (string): アセット名（hex）

**例:**

```typescript
import { Serializer } from '@hydra-sdk/core'

const unit = Serializer.serializeAssetUnit('a1b2c3...', '48656c6c6f')
console.log('Asset unit:', unit)
```

**戻り値:** `string`
アセットユニット（policyId + assetName）

---

### **`Deserializer.deserializeTx(txCborHex: string) → FixedTransaction`**

トランザクションの CBOR hex 文字列を `CardanoWASM.FixedTransaction` にデシリアライズします。

**パラメータ:**

- `txCborHex` (string): トランザクションの CBOR hex 文字列

**例:**

```typescript
import { Deserializer } from '@hydra-sdk/core'

const tx = Deserializer.deserializeTx(cborHex)
console.log('Tx hash:', tx.transaction_hash().to_hex())
```

**戻り値:** `FixedTransaction`
解析されたトランザクションオブジェクト

---

### **`Deserializer.deserializeAmountsFromTx(cborHex: string) → Asset[]`** <Badge type="tip" text="New in v1.4.0" />

トランザクションのすべての出力にまたがるすべての金額（lovelace + ネイティブトークン）を unit ごとに統合して返します。

**パラメータ:**

- `cborHex` (string): トランザクションの CBOR hex 文字列

**例:**

```typescript
import { Deserializer } from '@hydra-sdk/core'

const amounts = Deserializer.deserializeAmountsFromTx(cborHex)
console.log('Amounts:', amounts)
// => [{ unit: 'lovelace', quantity: '5000000' }, ...]
```

**戻り値:** `Asset[]`
unit ごとに統合された `{ unit, quantity }` エントリの配列

---

### **`Resolver.resolveTxHash(cborHex: string) → string`**

トランザクションの CBOR hex 文字列からトランザクションハッシュを解決します。

**パラメータ:**

- `cborHex` (string): トランザクションの CBOR hex 文字列

**例:**

```typescript
import { Resolver } from '@hydra-sdk/core'

const txHash = Resolver.resolveTxHash(cborHex)
console.log('Transaction hash:', txHash)
```

**戻り値:** `string`
トランザクションハッシュ（hex）

---

### **`Resolver.resolveTxBodyHash(txBody: TransactionBody) → TransactionHash`**

`TransactionBody` からトランザクションハッシュを解決します。

**パラメータ:**

- `txBody` (TransactionBody): トランザクションボディオブジェクト

**例:**

```typescript
import { Resolver } from '@hydra-sdk/core'

const hash = Resolver.resolveTxBodyHash(txBody)
console.log('Transaction hash:', hash.to_hex())
```

**戻り値:** `TransactionHash`
トランザクションハッシュオブジェクト

---

### **`Converter.convertUTxOObjectToUTxO(utxoObject: UTxOObject) → UTxO[]`**

スナップショット形式の UTxO オブジェクトを `UTxO` アイテムの配列に変換します。逆方向の変換には `Converter.convertUTxOToUTxOObject(utxos)` を、datum デシリアライズキャッシュを調整するには `Converter.convertUTxOObjectToUTxOWithOptions(utxoObject, options)` を使用してください。

**パラメータ:**

- `utxoObject` (UTxOObject): スナップショット UTxO オブジェクト

**例:**

```typescript
import { Converter } from '@hydra-sdk/core'

const utxos = Converter.convertUTxOObjectToUTxO(snapshotUtxo)
console.log('UTxO count:', utxos.length)

// 逆方向の変換
const snapshot = Converter.convertUTxOToUTxOObject(utxos)
```

**戻り値:** `UTxO[]`
UTxO の配列

> **Hex 変換ヘルパー** は `ParserUtils` にあります（[Data Parsing](#data-parsing) 参照）— `ParserUtils.bytesToHex` / `ParserUtils.hexToBytes` を使用してください。`Converter` は生の hex ではなく UTxO の形状を扱います。

---

### **`BuildKeys.buildKeys(privateKeyHex: string | [string, string], accountIndex: number, keyIndex?: number) → { accountKey, paymentKey, stakeKey, dRepKey? }`**

BIP-44 派生パス `m/1852'/1815'/accountIndex'/role/keyIndex` を使用して、ルートプライベートキーから account、payment、stake、dRep キーを派生します。

**パラメータ:**

- `privateKeyHex` (string | [string, string]): ルートプライベートキー hex（または `[accountKey, stakeKey]` のペア）
- `accountIndex` (number): アカウントインデックス
- `keyIndex` (number, optional): キーインデックス（デフォルト: 0）

**例:**

```typescript
import { BuildKeys } from '@hydra-sdk/core'

const { accountKey, paymentKey, stakeKey } = BuildKeys.buildKeys(rootKeyHex, 0, 0)
console.log('Payment key:', paymentKey.to_hex())
```

**戻り値:** `{ accountKey, paymentKey, stakeKey, dRepKey? }`
派生された `Bip32PrivateKey` 値

---

### **`BuildKeys.buildBaseAddress(networkId: number, paymentKeyHash, stakeKeyHash) → BaseAddress`**

payment と stake のキーハッシュからベースアドレス（payment + staking）を構築します。関連ヘルパー `buildEnterpriseAddress` と `buildRewardAddress` も利用できます。

**パラメータ:**

- `networkId` (number): ネットワーク ID（例: `NETWORK_ID.PREPROD`）
- `paymentKeyHash` (Ed25519KeyHash): payment キーハッシュ
- `stakeKeyHash` (Ed25519KeyHash): stake キーハッシュ

**例:**

```typescript
import { BuildKeys, NETWORK_ID } from '@hydra-sdk/core'

const baseAddress = BuildKeys.buildBaseAddress(
	NETWORK_ID.PREPROD,
	paymentKeyHash,
	stakeKeyHash
)
console.log('Base address:', baseAddress.to_address().to_bech32())
```

**戻り値:** `BaseAddress`
ベースアドレスオブジェクト

---

## Cost Models

### **`CostModels.buildCostModels(models: { plutusV1?: number[]; plutusV2?: number[]; plutusV3?: number[] }) → Costmdls`**

Plutusスクリプト用のコストモデルを構築します。`plutusV1` / `plutusV2` / `plutusV3` のうち少なくとも1つを指定する必要があります。指定しない場合は `At least one cost model must be provided` がスローされます。設定不要のデフォルトが必要な場合は `CostModels.defaultCostModels` を使用してください。

**パラメータ:**

- `models` (object): Plutus 言語バージョンごとのコストモデルリスト

**例:**

```typescript
import { CostModels } from '@hydra-sdk/core'

// 3つの Plutus バージョンすべてのすぐに使えるデフォルト
const defaults = CostModels.defaultCostModels

// または明示的なコストモデルリストから構築
const costModels = CostModels.buildCostModels({
	plutusV3: [/* ...コストモデル整数... */]
})
console.log('Cost Models:', costModels)
```

**戻り値:** `Costmdls`
コストモデルオブジェクト

---

## 完全な例

### 基本的なデータ変換

```typescript
import {
	ParserUtils,
	DatumUtils,
	PolicyUtils,
	SLOT_CONFIG_NETWORK,
	TimeUtils
} from '@hydra-sdk/core'

// データを変換
const hex = ParserUtils.stringToHex('Hello Cardano')
const bytes = ParserUtils.hexToBytes(hex)
console.log('Hex:', hex)
console.log('Bytes:', bytes)

// 時間を扱う
const currentSlot = TimeUtils.unixTimeToEnclosingSlot(
	Math.floor(Date.now() / 1000),
	SLOT_CONFIG_NETWORK.PREPROD
)
const readableTime = new Date(
	TimeUtils.slotToBeginUnixTime(currentSlot, SLOT_CONFIG_NETWORK.PREPROD) * 1000
)
console.log('Current slot:', currentSlot)
console.log('Readable time:', readableTime.toLocaleString())

// datumを作成
const datum = DatumUtils.mkConstr(0, [
	DatumUtils.mkInt(42),
	DatumUtils.mkBytes(hex)
])
console.log('Datum hex:', datum.to_hex())
```

### キーの操作

```typescript
import { KeysUtils } from '@hydra-sdk/core'

// Cardano CLIキーを生成
const cardanoKeys = KeysUtils.cardanoCliKeygen()
console.log({
	signingKey: cardanoKeys.sk.cborHex,
	verificationKey: cardanoKeys.vk.cborHex
})

// Hydraキーを生成
const hydraKeys = KeysUtils.hydraCliKeygen()
console.log({
	hydraSigningKey: hydraKeys.sk.cborHex,
	hydraVkey: hydraKeys.vk.cborHex
})

// 署名キーから検証キーを生成
const vkey = KeysUtils.genVkey(cardanoKeys.sk)
console.log('Verification Key:', vkey.cborHex)
```

### ミントポリシーの作成

```typescript
import { PolicyUtils, ParserUtils } from '@hydra-sdk/core'

// キーハッシュからポリシーを構築
const keyHash = '...' // あなたの公開鍵ハッシュ
const policyScript = PolicyUtils.buildMintingPolicyScriptFromKeyHash(keyHash)

// Policy IDを抽出
const policyId = PolicyUtils.policyIdFromNativeScript(policyScript)
console.log('Policy ID:', policyId)

// アドレスからポリシーを構築
const address = 'addr1qx2fxv2...'
const addressPolicy = PolicyUtils.buildMintingPolicyScriptFromAddress(address)
const addressPolicyId = PolicyUtils.policyIdFromNativeScript(addressPolicy)
console.log('Address Policy ID:', addressPolicyId)
```

### メタデータの処理

```typescript
import { MetadataUtils } from '@hydra-sdk/core'

// NFTメタデータ
const nftMetadata = MetadataUtils.metadataObjToMetadatum({
	name: 'My NFT',
	image: 'ipfs://QmXxxx...',
	description: 'A beautiful NFT',
	attributes: [
		{ trait_type: 'Rarity', value: 'Rare' },
		{ trait_type: 'Color', value: 'Blue' }
	]
})

// 数値とバイトを含むカスタムメタデータ
const customMetadata = MetadataUtils.metadataObjToMetadatum(
	new Map([
		['owner', 'Alice'],
		['value', 1000000],
		['hash', new Uint8Array([0x12, 0x34, 0x56, 0x78])]
	])
)

console.log('NFT Metadata:', nftMetadata)
console.log('Custom Metadata:', customMetadata)
```

### Providerを使用したブロックチェーンクエリ

```typescript
import { ProviderUtils } from '@hydra-sdk/core'

// Blockfrostを使用
const blockfrostProvider = new ProviderUtils.BlockfrostProvider({
	apiKey: 'your-blockfrost-project-id',
	network: 'preprod'
})

// fetcher 経由で UTxO をクエリ（プロバイダーに getUtxos/getProtocolParameters はありません）
const utxos = await blockfrostProvider.fetcher.fetchAddressUTxOs(address)
console.log('UTxOs:', utxos)

// submitter 経由で送信
// const txHash = await blockfrostProvider.submitter.submitTx(signedTxHex)

// Ogmiosを使用
const ogmiosProvider = new ProviderUtils.OgmiosProvider({
	network: 'preprod',
	apiEndpoint: 'http://localhost:1337'
})

const ogmiosUtxos = await ogmiosProvider.fetcher.fetchAddressUTxOs(address)
console.log('Ogmios UTxOs:', ogmiosUtxos)
```

---

## 型定義

すべてのユーティリティには包括的なTypeScript定義が付属しています：

```typescript
import type {
	SlotConfig,
	Network,
	PlutusData,
	ConstrPlutusData,
	PlutusMap,
	CardanoCLiSkey,
	CardanoCLiVkey,
	HydraCliSkey,
	HydraCliVkey,
	TransactionMetadatum
} from '@hydra-sdk/core'
```

---

## 関連ドキュメント

- [Core API](/ja/2.api/1.core) - ウォレットと認証
- [Transaction Builder API](/ja/2.api/3.transaction) - トランザクション構築
- [Bridge API](/ja/2.api/2.bridge) - Hydra Layer 2統合

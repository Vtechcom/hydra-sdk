---
title: '@hydra-sdk/core'
description: Core Cardano wallet API with comprehensive utilities
---

# @hydra-sdk/core

## Functions

---

## AppWallet

### **`AppWallet.brew(strength?: number) → string`**

Tạo mới một cụm từ mnemonic ngẫu nhiên cho ví Cardano mới.

**Parameters:**
- `strength?` (number): Độ mạnh của mnemonic (128 = 12 từ, 160 = 15 từ, 256 = 24 từ). Mặc định: 256 (24 từ)

**Example:**
```typescript
import { AppWallet } from '@hydra-sdk/core'

// Tạo mnemonic 24 từ (mặc định)
const mnemonic24 = AppWallet.brew(256)

// Tạo mnemonic 12 từ
const mnemonic12 = AppWallet.brew(128)

// Tạo mnemonic 15 từ
const mnemonic15 = AppWallet.brew(160)

console.log('Mnemonic mới:', mnemonic24)
// => "abandon abandon ... zone"
```

**Returns:** `string`  
Cụm từ mnemonic ngẫu nhiên được tách bằng dấu cách

---

### **`AppWallet.getAccount(accountIndex?: number, keyIndex?: number) → Account`**

Lấy thông tin tài khoản với các khóa và địa chỉ đã được sinh ra.

**Parameters:**
- `accountIndex?` (number): Chỉ mục tài khoản (theo BIP-44). Mặc định: 0
- `keyIndex?` (number): Chỉ mục địa chỉ. Mặc định: 0

**Example:**
```typescript
import { AppWallet, NETWORK_ID, ProviderUtils } from '@hydra-sdk/core'

const blockfrostProvider = new ProviderUtils.BlockfrostProvider({
  apiKey: 'your-blockfrost-api-key',
  network: 'preprod'
})

const wallet = new AppWallet({
  networkId: NETWORK_ID.PREPROD,
  key: { type: 'mnemonic', words: ['word1', 'word2', ...] },
  fetcher: blockfrostProvider.fetcher,
  submitter: blockfrostProvider.submitter
})

// Lấy tài khoản đầu tiên (account 0, address 0)
const account = wallet.getAccount(0, 0)

console.log({
  // Thông tin địa chỉ
  baseAddressBech32: account.baseAddressBech32,
  enterpriseAddressBech32: account.enterpriseAddressBech32,
  rewardAddressBech32: account.rewardAddressBech32,
  
  // Thông tin khóa
  paymentKey: account.paymentKey,
  stakeKey: account.stakeKey,
  
  // Chỉ mục
  accountIndex: account.accountIndex,
  addressIndex: account.addressIndex
})

// Mặc định: tài khoản 0, địa chỉ 0
const defaultAccount = wallet.getAccount()
```

**Returns:** `Account`
- `baseAddressBech32`: Địa chỉ cơ sở (thanh toán + staking)
- `enterpriseAddressBech32`: Địa chỉ doanh nghiệp (chỉ thanh toán)
- `rewardAddressBech32`: Địa chỉ phần thưởng (chỉ staking)
- `paymentKey`: Khóa thanh toán
- `stakeKey`: Khóa staking
- `accountIndex`: Chỉ mục tài khoản
- `addressIndex`: Chỉ mục địa chỉ

---

### **`AppWallet.getPaymentAddress(accountIndex?: number, keyIndex?: number) → string`**

Lấy địa chỉ thanh toán cơ sở (bao gồm cả staking).

**Parameters:**
- `accountIndex?` (number): Chỉ mục tài khoản. Mặc định: 0
- `keyIndex?` (number): Chỉ mục địa chỉ. Mặc định: 0

**Example:**
```typescript
const baseAddr = wallet.getPaymentAddress(0, 0)
console.log('Địa chỉ cơ sở:', baseAddr)
// => "addr_test1qz2fxv2umyhttkxyxp8x0..."
```

**Returns:** `string`  
Địa chỉ cơ sở dưới định dạng bech32

---

### **`AppWallet.getEnterpriseAddress(accountIndex?: number, keyIndex?: number) → string`**

Lấy địa chỉ doanh nghiệp (chỉ thanh toán, không có staking).

**Parameters:**
- `accountIndex?` (number): Chỉ mục tài khoản. Mặc định: 0
- `keyIndex?` (number): Chỉ mục địa chỉ. Mặc định: 0

**Example:**
```typescript
const enterpriseAddr = wallet.getEnterpriseAddress(0, 0)
console.log('Địa chỉ doanh nghiệp:', enterpriseAddr)
// => "addr_test1vz2fxv2umyhttkxyxp8x0..."
```

**Returns:** `string`  
Địa chỉ doanh nghiệp dưới định dạng bech32

---

### **`AppWallet.getRewardAddress(accountIndex?: number, keyIndex?: number) → string`**

Lấy địa chỉ phần thưởng (chỉ staking).

**Parameters:**
- `accountIndex?` (number): Chỉ mục tài khoản. Mặc định: 0
- `keyIndex?` (number): Chỉ mục địa chỉ. Mặc định: 0

**Example:**
```typescript
const rewardAddr = wallet.getRewardAddress(0, 0)
console.log('Địa chỉ phần thưởng:', rewardAddr)
// => "stake_test1uqz2fxv2umyhttkxyxp8x0..."
```

**Returns:** `string`  
Địa chỉ phần thưởng dưới định dạng bech32

---

### **`AppWallet.getNetworkId() → number`**

Lấy ID mạng hiện tại của ví.

**Example:**
```typescript
const networkId = wallet.getNetworkId()
console.log('Network ID:', networkId)
// => 0 (Preprod testnet) hoặc 1 (Mainnet)
```

**Returns:** `number`
- `0`: Testnet (Preprod)
- `1`: Mainnet

---

### **`AppWallet.signTx(txCbor: string, partialSign?: boolean, accountIndex?: number, keyIndex?: number) → Promise<string>`**

Ký một giao dịch Cardano. Hỗ trợ ký hoàn toàn hoặc ký một phần (multi-sig).

**Parameters:**
- `txCbor` (string): Giao dịch dưới dạng CBOR hex
- `partialSign?` (boolean): Nếu true, ký một phần. Mặc định: false
- `accountIndex?` (number): Chỉ mục tài khoản. Mặc định: 0
- `keyIndex?` (number): Chỉ mục địa chỉ. Mặc định: 0

**Example:**
```typescript
// Ký hoàn toàn
const signedTx = await wallet.signTx(unsignedTxCbor, false, 0, 0)

// Ký một phần (multi-sig)
const partialSigned = await wallet.signTx(unsignedTxCbor, true, 0, 0)

console.log('Giao dịch đã ký:', signedTx)
```

**Returns:** `Promise<string>`  
Giao dịch đã ký dưới dạng CBOR hex

---

### **`AppWallet.signTxs(txCbors: string[], partialSign?: boolean) → Promise<string[]>`**

Ký nhiều giao dịch cùng một lúc.

**Parameters:**
- `txCbors` (string[]): Mảng giao dịch dưới dạng CBOR hex
- `partialSign?` (boolean): Nếu true, ký một phần. Mặc định: false

**Example:**
```typescript
const signedTxs = await wallet.signTxs(
  [txCbor1, txCbor2, txCbor3],
  false
)

console.log('Giao dịch đã ký:', signedTxs)
```

**Returns:** `Promise<string[]>`  
Mảng giao dịch đã ký dưới dạng CBOR hex

---

### **`AppWallet.signData(address: string, payload: string, accountIndex?: number, keyIndex?: number) → Promise<{ signature: string; key: string }>`**

Ký dữ liệu tùy ý theo CIP-8 (Cardano Improvement Proposal).

**Parameters:**
- `address` (string): Địa chỉ Cardano (bech32)
- `payload` (string): Dữ liệu cần ký
- `accountIndex?` (number): Chỉ mục tài khoản. Mặc định: 0
- `keyIndex?` (number): Chỉ mục địa chỉ. Mặc định: 0

**Example:**
```typescript
const signature = await wallet.signData(
  'addr_test1qz2fxv2umyhttkxyxp8x0...',
  'Hello World',
  0,
  0
)

console.log('Chữ ký dữ liệu:', signature)
// => { signature: '...', key: '...' }
```

**Returns:** `Promise<{ signature: string; key: string }>`
- `signature`: Chữ ký dưới dạng hex
- `key`: Khóa công khai dưới dạng hex

---

### **`AppWallet.submitTx(signedTxCbor: string) → Promise<string>`**

Gửi một giao dịch đã ký lên blockchain.

**Parameters:**
- `signedTxCbor` (string): Giao dịch đã ký dưới dạng CBOR hex

**Example:**
```typescript
const txHash = await wallet.submitTx(signedTxCbor)
console.log('Hash giao dịch:', txHash)
// => "a1b2c3d4e5f6..."
```

**Returns:** `Promise<string>`  
Hash giao dịch

---

### **`AppWallet.queryUTxOs(address: string) → Promise<UTxO[]>`**

Truy vấn UTxOs (Unspent Transaction Outputs) cho một địa chỉ.

**Parameters:**
- `address` (string): Địa chỉ Cardano (bech32)

**Example:**
```typescript
const address = wallet.getPaymentAddress()
const utxos = await wallet.queryUTxOs(address)

console.log('UTxOs:', utxos)
// => [{ input: { txHash: '...', index: 0 }, output: { address: '...', amount: [...] } }, ...]
```

**Returns:** `Promise<UTxO[]>`  
Mảng UTxOs cho địa chỉ

---

## EmbeddedWallet

Thực hiện wallet ở mức thấp được sử dụng nội bộ bởi AppWallet.

### **`EmbeddedWallet.generateMnemonic(strength?: number) → string`**

Tạo mnemonic phrase.

**Parameters:**
- `strength?` (number): Độ mạnh mnemonic (128/160/256). Mặc định: 256

**Example:**
```typescript
import { EmbeddedWallet } from '@hydra-sdk/core'

const mnemonic = EmbeddedWallet.generateMnemonic(256)
console.log('Mnemonic:', mnemonic)
```

**Returns:** `string`  
Cụm từ mnemonic

---

### **`EmbeddedWallet.mnemonicToPrivateKeyHex(words: string[], password?: string) → string`**

Chuyển đổi mnemonic thành khóa riêng tư hex.

**Parameters:**
- `words` (string[]): Mảng các từ mnemonic
- `password?` (string): Mật khẩu tuỳ chọn

**Example:**
```typescript
const privateKeyHex = EmbeddedWallet.mnemonicToPrivateKeyHex(
  ['abandon', 'abandon', ...],
  ''
)
console.log('Khóa riêng:', privateKeyHex)
```

**Returns:** `string`  
Khóa riêng tư dưới dạng hex

---

### **`EmbeddedWallet.privateKeyHexToBech32(privateKeyHex: string) → string`**

Chuyển đổi khóa riêng tư hex sang bech32.

**Parameters:**
- `privateKeyHex` (string): Khóa riêng tư dưới dạng hex

**Example:**
```typescript
const bech32Key = EmbeddedWallet.privateKeyHexToBech32(privateKeyHex)
console.log('Khóa bech32:', bech32Key)
```

**Returns:** `string`  
Khóa riêng tư dưới dạng bech32 (xprv)

---

### **`EmbeddedWallet.privateKeyBech32ToPrivateKeyHex(bech32Key: string) → string`**

Chuyển đổi khóa riêng tư bech32 sang hex.

**Parameters:**
- `bech32Key` (string): Khóa riêng tư dưới dạng bech32

**Example:**
```typescript
const hexKey = EmbeddedWallet.privateKeyBech32ToPrivateKeyHex('xprv1...')
console.log('Khóa hex:', hexKey)
```

**Returns:** `string`  
Khóa riêng tư dưới dạng hex

---

## CardanoCliWallet

Lớp để làm việc với các khóa được tạo từ Cardano CLI. Hữu ích cho các ứng dụng phía máy chủ.

### **`CardanoCliWallet.getAddressBech32() → string`**

Lấy địa chỉ ví dưới định dạng bech32.

**Example:**
```typescript
import { CardanoCliWallet, NETWORK_ID, ProviderUtils } from '@hydra-sdk/core'

const blockfrostProvider = new ProviderUtils.BlockfrostProvider({
  apiKey: 'your-blockfrost-api-key',
  network: 'preprod'
})

const cliWallet = new CardanoCliWallet({
  networkId: NETWORK_ID.PREPROD,
  skey: '5820245120cdf333f8ea6910922b3f05bcbc0d5c8e8486ca94c020623d5cca822e04',
  vkey: '5820216f72947d1b97d56825c5f9f8a2e6f14234c02171853264f2f552a2685b25e0',
  fetcher: blockfrostProvider.fetcher,
  submitter: blockfrostProvider.submitter
})

const address = cliWallet.getAddressBech32()
console.log('Địa chỉ ví:', address)
```

**Returns:** `string`  
Địa chỉ dưới định dạng bech32

---

### **`CardanoCliWallet.getNetworkId() → number`**

Lấy ID mạng hiện tại.

**Example:**
```typescript
const networkId = cliWallet.getNetworkId()
console.log('Network ID:', networkId) // 0 hoặc 1
```

**Returns:** `number`
- `0`: Testnet
- `1`: Mainnet

---

### **`CardanoCliWallet.signTx(txCbor: string, partialSign?: boolean) → Promise<string>`**

Ký một giao dịch.

**Parameters:**
- `txCbor` (string): Giao dịch dưới dạng CBOR hex
- `partialSign?` (boolean): Ký một phần. Mặc định: false

**Example:**
```typescript
const signedTx = await cliWallet.signTx(unsignedTxCbor, false)
console.log('Giao dịch đã ký:', signedTx)
```

**Returns:** `Promise<string>`  
Giao dịch đã ký dưới dạng CBOR hex

---

### **`CardanoCliWallet.submitTx(txCbor: string) → Promise<string>`**

Gửi giao dịch đã ký lên blockchain.

**Parameters:**
- `txCbor` (string): Giao dịch đã ký dưới dạng CBOR hex

**Example:**
```typescript
const txHash = await cliWallet.submitTx(signedTxCbor)
console.log('Hash giao dịch:', txHash)
```

**Returns:** `Promise<string>`  
Hash giao dịch

---

### **`CardanoCliWallet.queryUTxOs(address: string) → Promise<UTxO[]>`**

Truy vấn UTxOs cho một địa chỉ.

**Parameters:**
- `address` (string): Địa chỉ Cardano

**Example:**
```typescript
const address = cliWallet.getAddressBech32()
const utxos = await cliWallet.queryUTxOs(address)
console.log('UTxOs:', utxos)
```

**Returns:** `Promise<UTxO[]>`  
Mảng UTxOs

---

## Provider Classes

### **`BlockfrostProvider`**

Nhà cung cấp Blockfrost để truy vấn dữ liệu blockchain.

**Constructor:**
```typescript
import { ProviderUtils } from '@hydra-sdk/core'

const blockfrostProvider = new ProviderUtils.BlockfrostProvider({
  apiKey: 'your-blockfrost-api-key',
  network: 'preprod' // hoặc 'mainnet'
})

// Truy cập fetcher và submitter
const fetcher = blockfrostProvider.fetcher
const submitter = blockfrostProvider.submitter

// Sử dụng với ví
const wallet = new AppWallet({
  networkId: NETWORK_ID.PREPROD,
  key: { type: 'mnemonic', words: mnemonic },
  fetcher: blockfrostProvider.fetcher,
  submitter: blockfrostProvider.submitter
})
```

---

### **`OgmiosProvider`**

Nhà cung cấp Ogmios để truy vấn dữ liệu blockchain.

**Constructor:**
```typescript
import { ProviderUtils } from '@hydra-sdk/core'

const ogmiosProvider = new ProviderUtils.OgmiosProvider({
  apiEndpoint: 'https://preprod.cardano-rpc.hydrawallet.app',
  network: 'preprod' // hoặc 'mainnet'
})

// Truy cập fetcher và submitter
const fetcher = ogmiosProvider.fetcher
const submitter = ogmiosProvider.submitter

// Sử dụng với ví
const wallet = new AppWallet({
  networkId: NETWORK_ID.PREPROD,
  key: { type: 'mnemonic', words: mnemonic },
  fetcher: ogmiosProvider.fetcher,
  submitter: ogmiosProvider.submitter
})
```

---

## Utility Functions

### **`getPubkeyHashFromAddress(address: string) → string`**

Trích xuất public key hash từ một địa chỉ Cardano.

**Parameters:**
- `address` (string): Địa chỉ Cardano (bech32)

**Example:**
```typescript
import { getPubkeyHashFromAddress } from '@hydra-sdk/core'

const pubkeyHash = getPubkeyHashFromAddress('addr_test1qz2fxv2umyhttkxyxp8x0...')
console.log('Public key hash:', pubkeyHash)
// => "00000000000000000000000000000000000000000000000000000000"
```

**Returns:** `string`  
Public key hash dưới dạng hex

---

### **`isValidAddress(address: string) → boolean`**

Kiểm tra xem một địa chỉ Cardano có hợp lệ không.

**Parameters:**
- `address` (string): Địa chỉ cần kiểm tra

**Example:**
```typescript
import { isValidAddress } from '@hydra-sdk/core'

const isValid = isValidAddress('addr_test1qz2fxv2umyhttkxyxp8x0...')
console.log('Hợp lệ:', isValid) // => true hoặc false
```

**Returns:** `boolean`  
True nếu địa chỉ hợp lệ, false nếu không

---

### **`isValidTxOutput(output: any) → boolean`**

Kiểm tra xem một output giao dịch có hợp lệ không.

**Parameters:**
- `output` (any): Output giao dịch cần kiểm tra

**Example:**
```typescript
import { isValidTxOutput } from '@hydra-sdk/core'

const isValid = isValidTxOutput(txOutput)
console.log('Output hợp lệ:', isValid)
```

**Returns:** `boolean`  
True nếu output hợp lệ

---

### **`slotToBeginUnixTime(slot: number, config?: SlotConfig) → number`**

Chuyển đổi số slot thành thời gian Unix bắt đầu của slot đó.

**Parameters:**
- `slot` (number): Số slot
- `config?` (SlotConfig): Cấu hình slot tuỳ chọn

**Example:**
```typescript
import { slotToBeginUnixTime } from '@hydra-sdk/core'

const unixTime = slotToBeginUnixTime(45700800)
console.log('Thời gian Unix:', unixTime)
// => 1671292800000 (milliseconds)
```

**Returns:** `number`  
Thời gian Unix bắt đầu (milliseconds)

---

### **`unixTimeToEnclosingSlot(unixTime: number, config?: SlotConfig) → number`**

Chuyển đổi thời gian Unix thành slot bao quanh nó.

**Parameters:**
- `unixTime` (number): Thời gian Unix (milliseconds)
- `config?` (SlotConfig): Cấu hình slot tuỳ chọn

**Example:**
```typescript
import { unixTimeToEnclosingSlot } from '@hydra-sdk/core'

const slot = unixTimeToEnclosingSlot(Date.now())
console.log('Slot hiện tại:', slot)
```

**Returns:** `number`  
Số slot bao quanh thời gian Unix

---

### **`resolveSlotNo(timestamp: number) → string`**

Giải quyết số slot dựa trên timestamp (milliseconds).

**Parameters:**
- `timestamp` (number): Timestamp (milliseconds)

**Example:**
```typescript
import { resolveSlotNo } from '@hydra-sdk/core'

const slotNo = resolveSlotNo(Date.now())
console.log('Số slot:', slotNo)
```

**Returns:** `string`  
Số slot dưới dạng string

---

### **`resolveEpochNo(timestamp: number) → number`**

Giải quyết số epoch dựa trên timestamp (milliseconds).

**Parameters:**
- `timestamp` (number): Timestamp (milliseconds)

**Example:**
```typescript
import { resolveEpochNo } from '@hydra-sdk/core'

const epochNo = resolveEpochNo(Date.now())
console.log('Số epoch:', epochNo)
```

**Returns:** `number`  
Số epoch

---

### **`buildHydraSlotConfig(timestamp?: number) → SlotConfig`**

Xây dựng cấu hình slot Hydra cho một timestamp cụ thể.

**Parameters:**
- `timestamp?` (number): Timestamp (milliseconds). Mặc định: thời gian hiện tại

**Example:**
```typescript
import { buildHydraSlotConfig } from '@hydra-sdk/core'

const config = buildHydraSlotConfig()
console.log('Cấu hình slot:', config)
```

**Returns:** `SlotConfig`  
Cấu hình slot cho Hydra

---

### **`buildPolicyScriptFromPubkey(pubkey: string) → string`**

Xây dựng script chính sách minting từ một script khóa công khai.

**Parameters:**
- `pubkey` (string): Khóa công khai hex

**Example:**
```typescript
import { buildPolicyScriptFromPubkey } from '@hydra-sdk/core'

const policyScript = buildPolicyScriptFromPubkey('a1b2c3...')
console.log('Script chính sách:', policyScript)
```

**Returns:** `string`  
Script chính sách dưới dạng hex

---

### **`buildMintingPolicyScriptFromAddress(address: string) → string`**

Xây dựng script chính sách minting từ một địa chỉ.

**Parameters:**
- `address` (string): Địa chỉ Cardano (bech32)

**Example:**
```typescript
import { buildMintingPolicyScriptFromAddress } from '@hydra-sdk/core'

const policyScript = buildMintingPolicyScriptFromAddress('addr_test1...')
console.log('Script chính sách:', policyScript)
```

**Returns:** `string`  
Script chính sách dưới dạng hex

---

### **`buildMintingPolicyScriptFromKeyHash(keyHash: string) → string`**

Xây dựng script chính sách minting từ hash khóa.

**Parameters:**
- `keyHash` (string): Hash khóa hex

**Example:**
```typescript
import { buildMintingPolicyScriptFromKeyHash } from '@hydra-sdk/core'

const policyScript = buildMintingPolicyScriptFromKeyHash('a1b2c3...')
console.log('Script chính sách:', policyScript)
```

**Returns:** `string`  
Script chính sách dưới dạng hex

---

### **`policyIdFromNativeScript(nativeScript: string) → string`**

Trích xuất policy ID từ một native script.

**Parameters:**
- `nativeScript` (string): Native script dưới dạng hex

**Example:**
```typescript
import { policyIdFromNativeScript } from '@hydra-sdk/core'

const policyId = policyIdFromNativeScript(nativeScriptHex)
console.log('Policy ID:', policyId)
```

**Returns:** `string`  
Policy ID dưới dạng hex

---

### **`bytesToHex(bytes: Uint8Array) → string`**

Chuyển đổi bytes sang hex string.

**Parameters:**
- `bytes` (Uint8Array): Dữ liệu bytes

**Example:**
```typescript
import { bytesToHex } from '@hydra-sdk/core'

const hexString = bytesToHex(new Uint8Array([1, 2, 3]))
console.log('Hex:', hexString) // => "010203"
```

**Returns:** `string`  
Hex string

---

### **`hexToBytes(hex: string) → Uint8Array`**

Chuyển đổi hex string sang bytes.

**Parameters:**
- `hex` (string): Hex string

**Example:**
```typescript
import { hexToBytes } from '@hydra-sdk/core'

const bytes = hexToBytes('010203')
console.log('Bytes:', bytes) // => Uint8Array [ 1, 2, 3 ]
```

**Returns:** `Uint8Array`  
Dữ liệu bytes

---

### **`stringToHex(str: string) → string`**

Chuyển đổi UTF-8 string sang hex string.

**Parameters:**
- `str` (string): UTF-8 string

**Example:**
```typescript
import { stringToHex } from '@hydra-sdk/core'

const hex = stringToHex('Hello')
console.log('Hex:', hex) // => "48656c6c6f"
```

**Returns:** `string`  
Hex string

---

### **`hexToString(hex: string) → string`**

Chuyển đổi hex string sang UTF-8 string.

**Parameters:**
- `hex` (string): Hex string

**Example:**
```typescript
import { hexToString } from '@hydra-sdk/core'

const str = hexToString('48656c6c6f')
console.log('String:', str) // => "Hello"
```

**Returns:** `string`  
UTF-8 string

---

### **`toBytes(data: string | Uint8Array) → Uint8Array`**

Chuyển đổi hex string hoặc UTF-8 string sang bytes.

**Parameters:**
- `data` (string | Uint8Array): Hex string hoặc UTF-8 string

**Example:**
```typescript
import { toBytes } from '@hydra-sdk/core'

const bytes1 = toBytes('48656c6c6f')
const bytes2 = toBytes('Hello')
```

**Returns:** `Uint8Array`  
Dữ liệu bytes

---

### **`fromUTF8(str: string) → string`**

Chuyển đổi UTF-8 string sang hex string.

**Parameters:**
- `str` (string): UTF-8 string

**Example:**
```typescript
import { fromUTF8 } from '@hydra-sdk/core'

const hex = fromUTF8('Hello')
console.log('Hex:', hex)
```

**Returns:** `string`  
Hex string

---

### **`toUTF8(hex: string) → string`**

Chuyển đổi hex string sang UTF-8 string.

**Parameters:**
- `hex` (string): Hex string

**Example:**
```typescript
import { toUTF8 } from '@hydra-sdk/core'

const str = toUTF8('48656c6c6f')
console.log('String:', str)
```

**Returns:** `string`  
UTF-8 string

---

### **`metadataObjToMetadatum(obj: any) → TransactionMetadatum`**

Chuyển đổi đối tượng metadata sang CardanoWASM.TransactionMetadatum.

**Note:** 
- Kích thước metadata `bytes` tối đa: 64 byte
- Kích thước metadata `text` tối đa: 64 ký tự

**Parameters:**
- `obj` (any): Đối tượng metadata

**Example:**
```typescript
import { metadataObjToMetadatum } from '@hydra-sdk/core'

const metadata = metadataObjToMetadatum({
  '1': 'Hello',
  '2': 12345
})
console.log('Metadata:', metadata)
```

**Returns:** `TransactionMetadatum`  
Metadata dạng CardanoWASM

---

### **`cardanoCliKeygen() → { sk: CardanoCLiSkey; vk: CardanoCLiVkey }`**

Tạo cặp khóa ed25519 tương thích với Cardano CLI.

**Example:**
```typescript
import { cardanoCliKeygen } from '@hydra-sdk/core'

const { sk, vk } = cardanoCliKeygen()
console.log('Secret Key (cborHex):', sk.cborHex)
console.log('Verification Key (cborHex):', vk.cborHex)
```

**Returns:** `{ sk: CardanoCLiSkey; vk: CardanoCLiVkey }`
- `sk.cborHex`: Khóa ký dưới dạng CBOR hex
- `vk.cborHex`: Khóa xác minh dưới dạng CBOR hex

---

### **`hydraCliKeygen() → { sk: object; vk: object }`**

Tạo cặp khóa ed25519 tương thích với Hydra CLI.

**Example:**
```typescript
import { hydraCliKeygen } from '@hydra-sdk/core'

const { sk, vk } = hydraCliKeygen()
console.log('Secret Key:', sk)
console.log('Verification Key:', vk)
```

**Returns:** `{ sk: object; vk: object }`  
Cặp khóa Hydra

---

### **`genVkey(sk: CardanoCLiSkey) → CardanoCLiVkey`**

Tạo khóa xác minh từ khóa ký.

**Parameters:**
- `sk` (CardanoCLiSkey): Khóa ký

**Example:**
```typescript
import { genVkey, cardanoCliKeygen } from '@hydra-sdk/core'

const { sk } = cardanoCliKeygen()
const vk = genVkey(sk)
console.log('Verification Key:', vk.cborHex)
```

**Returns:** `CardanoCLiVkey`  
Khóa xác minh

---

### **`mkInt(value: number | bigint) → PlutusData`**

Tạo PlutusData từ một số nguyên.

**Parameters:**
- `value` (number | bigint): Giá trị số nguyên

**Example:**
```typescript
import { mkInt } from '@hydra-sdk/core'

const plutusInt = mkInt(42)
console.log('PlutusData:', plutusInt)
```

**Returns:** `PlutusData`  
PlutusData biểu diễn số nguyên

---

### **`mkBytes(hex: string) → PlutusData`**

Tạo PlutusData từ hex string biểu diễn bytes.

**Parameters:**
- `hex` (string): Hex string

**Example:**
```typescript
import { mkBytes } from '@hydra-sdk/core'

const plutusBytes = mkBytes('a1b2c3')
console.log('PlutusData:', plutusBytes)
```

**Returns:** `PlutusData`  
PlutusData biểu diễn bytes

---

### **`mkConstr(index: number, fields: PlutusData[]) → PlutusData`**

Tạo PlutusData constructor.

**Parameters:**
- `index` (number): Chỉ mục constructor
- `fields` (PlutusData[]): Mảng PlutusData fields

**Example:**
```typescript
import { mkConstr, mkInt } from '@hydra-sdk/core'

const constr = mkConstr(0, [mkInt(1), mkInt(2)])
console.log('Constructor:', constr)
```

**Returns:** `PlutusData`  
PlutusData constructor

---

### **`mkMap(pairs: [PlutusData, PlutusData][]) → PlutusData`**

Tạo PlutusData map.

**Parameters:**
- `pairs` ([PlutusData, PlutusData][]): Mảng cặp key-value

**Example:**
```typescript
import { mkMap, mkInt, mkBytes } from '@hydra-sdk/core'

const map = mkMap([
  [mkInt(1), mkBytes('a1b2')],
  [mkInt(2), mkBytes('c3d4')]
])
console.log('Map:', map)
```

**Returns:** `PlutusData`  
PlutusData map

---

### **`buildCostModels() → Costmdls`**

Xây dựng cost models cho Cardano.

**Example:**
```typescript
import { buildCostModels } from '@hydra-sdk/core'

const costModels = buildCostModels()
console.log('Cost Models:', costModels)
```

**Returns:** `Costmdls`  
Cost models

---

## Tiện ích chuyển đổi UTxO

### **`convertUTxOObjectToUTxO(utxoObject: UTxOObject) → UTxO[]`**

Chuyển đổi một UTxO object (định dạng snapshot) thành mảng các phần tử `UTxO`.

**Ví dụ:**
```typescript
import { Converter } from '@hydra-sdk/core'

const utxos = Converter.convertUTxOObjectToUTxO(snapshotUtxo)
console.log('Số lượng UTxO:', utxos.length)
```

**Returns:** `UTxO[]`

---

### **`convertUTxOObjectToUTxOWithOptions(utxoObject: UTxOObject, options?: ConvertOptions) → UTxO[]`** <Badge type="tip" text="Mới trong v1.3.0" />

Tương tự `convertUTxOObjectToUTxO` nhưng nhận thêm một đối tượng tùy chọn để tinh chỉnh bộ nhớ cache giải mã datum nội bộ.

**Tham số:**
- `utxoObject` (UTxOObject): Đối tượng UTxO từ snapshot
- `options?`:
  - `maxDatumCacheSize?` (number): Số lượng tối đa các mục `PlutusData` đã giải mã được lưu vào cache. `0` vô hiệu hóa cache. Mặc định: `1024`

**Tại sao quan trọng:** Khi xử lý snapshot lớn (hàng nghìn UTxO) có chứa inline datums lặp lại, cache giúp tránh các lần gọi WASM giải mã dư thừa. Chính sách loại bỏ LRU giữ cho mức sử dụng bộ nhớ có giới hạn.

**Ví dụ:**
```typescript
import { Converter } from '@hydra-sdk/core'

// Mặc định — cache tối đa 1024 mục datum
const utxos = Converter.convertUTxOObjectToUTxOWithOptions(snapshotUtxo)

// Cache lớn hơn cho snapshot với nhiều inline datum độc nhất
const utxos = Converter.convertUTxOObjectToUTxOWithOptions(snapshotUtxo, {
	maxDatumCacheSize: 4096
})

// Vô hiệu hóa cache hoàn toàn
const utxos = Converter.convertUTxOObjectToUTxOWithOptions(snapshotUtxo, {
	maxDatumCacheSize: 0
})
```

**Returns:** `UTxO[]`

---

### **`convertUTxOToUTxOObject(utxos: UTxO[]) → UTxOObject`**

Chuyển đổi mảng `UTxO[]` trở lại định dạng đối tượng snapshot.

**Ví dụ:**
```typescript
import { Converter } from '@hydra-sdk/core'

const utxoObj = Converter.convertUTxOToUTxOObject(utxos)
```

**Returns:** `UTxOObject`

---

## Constants

### **`NETWORK_ID`**

Hằng số ID mạng cho các mạng Cardano khác nhau.

**Example:**
```typescript
import { NETWORK_ID } from '@hydra-sdk/core'

// Các network IDs khả dụng
NETWORK_ID.TESTNET  // 0
NETWORK_ID.PREPROD  // 0
NETWORK_ID.MAINNET  // 1
```

---

## Related Documentation

- [Transaction Builder API](/api/transaction) - Xây dựng giao dịch
- [Hydra Bridge API](/api/bridge) - Tích hợp Hydra Layer 2



---
title: '@hydra-sdk/bridge'
description: Tích hợp Hydra Layer 2 và quản lý vòng đời Head
---

# @hydra-sdk/bridge

## Classes

---

## HydraBridge

Class chính để kết nối và quản lý Hydra Heads.

### **`HydraBridge.constructor(options: HydraBridgeOptions) → HydraBridge`**

Khởi tạo một Hydra Bridge mới.

**Parameters:**

- `options` (HydraBridgeOptions):
  - `url?`: WebSocket URL (ví dụ: 'ws://localhost:4001')
  - `connector?`: Custom connector instance
  - `verbose?`: Bật chế độ verbose logging
  - `history?`: Bật nhận lịch sử node đầy đủ (mặc định: false)
  - `noSnapshotUtxo?`: Vô hiệu hóa hiển thị snapshot UTxO (mặc định: false)
  - `address?`: Lọc transaction outputs theo địa chỉ cụ thể

**Example:**

```typescript
import { HydraBridge } from '@hydra-sdk/bridge'

// Kết nối trực tiếp WebSocket
const bridge = new HydraBridge({
	url: 'ws://localhost:4001',
	verbose: true
})

// Với các tùy chọn nâng cao
const bridge = new HydraBridge({
	url: 'ws://localhost:4001',
	verbose: true,
	history: true,
	noSnapshotUtxo: false,
	address: 'addr_test1...'
})

// Kết nối Hydra Hub với API key
const bridge = new HydraBridge({
	url: 'wss://hydra-hub.example.com?X-Api-Key=your_api_key_here',
	verbose: true
})

// Sử dụng custom connector
const bridge = new HydraBridge({
	connector: customConnector,
	verbose: true
})
```

**Returns:** `HydraBridge`  
Instance HydraBridge mới

---

## Connection Management

### **`HydraBridge.connect() → Promise<void>`**

Kết nối tới Hydra Node.

**Example:**

```typescript
await bridge.connect()
console.log('Đã kết nối tới Hydra Node')
```

**Returns:** `Promise<void>`

---

### **`HydraBridge.disconnect() → Promise<void>`**

Ngắt kết nối từ Hydra Node.

**Example:**

```typescript
await bridge.disconnect()
console.log('Đã ngắt kết nối khỏi Hydra Node')
```

**Returns:** `Promise<void>`

---

### **`HydraBridge.connected() → boolean`**

Kiểm tra trạng thái kết nối.

**Example:**

```typescript
const isConnected = bridge.connected()
console.log('Trạng thái kết nối:', isConnected)
```

**Returns:** `boolean`  
True nếu đang kết nối, false nếu không

---

## Head Information

### **`HydraBridge.headInfo() → Promise<HeadInfo>`**

Lấy thông tin Head hiện tại.

**Example:**

```typescript
const info = await bridge.headInfo()
console.log({
	headId: info.headId,
	headStatus: info.headStatus,
	vkey: info.vkey
})
```

**Returns:** `Promise<HeadInfo>`  
Thông tin Head

---

## Protocol Parameters

### **`HydraBridge.getProtocolParameters() → Promise<Protocol>`**

Lấy tham số giao thức Cardano.

**Example:**

```typescript
const protocolParams = await bridge.getProtocolParameters()
console.log({
	minFeeA: protocolParams.min_fee_a,
	minFeeB: protocolParams.min_fee_b,
	maxTxSize: protocolParams.max_tx_size,
	keyDeposit: protocolParams.key_deposit,
	poolDeposit: protocolParams.pool_deposit
})
```

**Returns:** `Promise<Protocol>`  
Tham số giao thức

---

## UTxO Management

### **`HydraBridge.querySnapshotUtxo() → Promise<UTxOObject>`**

Truy vấn UTxOs trong snapshot hiện tại.

**Example:**

```typescript
const utxoObject = await bridge.querySnapshotUtxo()
console.log('Snapshot UTxO:', utxoObject)
```

**Returns:** `Promise<UTxOObject>`  
Object UTxO trong snapshot

---

### **`HydraBridge.snapshotUtxoArray() → UTxO[]`**

Lấy mảng UTxOs từ snapshot.

**Example:**

```typescript
const utxoArray = bridge.snapshotUtxoArray()
console.log('Mảng UTxO:', utxoArray)
```

**Returns:** `UTxO[]`  
Mảng UTxOs

---

### **`HydraBridge.queryAddressUTxO(address: string) → Promise<UTxO[]>`**

Truy vấn UTxOs cho một địa chỉ cụ thể.

**Parameters:**

- `address` (string): Địa chỉ Cardano (bech32)

**Example:**

```typescript
const account = wallet.getAccount(0, 0)
const addressUtxos = await bridge.queryAddressUTxO(account.baseAddressBech32)
console.log('Địa chỉ UTxOs:', addressUtxos)
```

**Returns:** `Promise<UTxO[]>`  
Mảng UTxOs cho địa chỉ

---

### **`HydraBridge.addressesInHead() → Promise<string[]>`**

Lấy tất cả địa chỉ trong Head hiện tại.

**Example:**

```typescript
const addresses = await bridge.addressesInHead()
console.log('Địa chỉ trong Head:', addresses)
```

**Returns:** `Promise<string[]>`  
Mảng địa chỉ

---

## Transaction Operations

### **`HydraBridge.submitTxSync(transaction: Transaction, options?: SubmitOptions) → Promise<TxResult>`**

Gửi giao dịch và chờ xác nhận.

**Parameters:**

- `transaction` (Transaction): Giao dịch cần gửi
- `options?` (SubmitOptions): Tuỳ chọn gửi
  - `timeout?`: Timeout (milliseconds)

**Example:**

```typescript
const result = await bridge.submitTxSync(
	{
		type: 'Witnessed Tx ConwayEra',
		description: 'Ledger Cddl Format',
		cborHex: signedTxCbor,
		txId: transactionId
	},
	{ timeout: 30000 }
)

console.log({
	txId: result.txId,
	isValid: result.isValid,
	isConfirmed: result.isConfirmed
})
```

**Returns:** `Promise<TxResult>`  
Kết quả giao dịch

---

### **`HydraBridge.commit(data: CommitData) → Promise<any>`**

Commit UTxOs vào Head.

**Parameters:**

- `data` (CommitData): Dữ liệu commit

**Example:**

```typescript
const result = await bridge.commit({
	cborHex: commitTxCbor,
	description: 'Commit transaction'
})
console.log('Kết quả commit:', result)
```

**Returns:** `Promise<any>`

---

### **`HydraBridge.submitCardanoTransaction(data: TransactionData) → Promise<any>`**

Gửi giao dịch tới mạng Cardano.

**Parameters:**

- `data` (TransactionData): Dữ liệu giao dịch

**Example:**

```typescript
const result = await bridge.submitCardanoTransaction({
	cborHex: txCbor,
	description: 'Cardano transaction'
})
console.log('Giao dịch Cardano đã gửi:', result)
```

**Returns:** `Promise<any>`

---

## Commands

### **`HydraBridge.commands.init() → void`**

Khởi tạo Hydra Head mới.

**Example:**

```typescript
bridge.commands.init()
```

---

### **`HydraBridge.commands.close() → void`**

Đóng Hydra Head.

**Example:**

```typescript
bridge.commands.close()
```

---

### **`HydraBridge.commands.abort() → void`**

Hủy bỏ khởi tạo Head.

**Example:**

```typescript
bridge.commands.abort()
```

---

### **`HydraBridge.commands.fanout() → void`**

Thực hiện fanout sau khi đóng Head.

**Example:**

```typescript
bridge.commands.fanout()
```

---

### **`HydraBridge.commands.contest() → void`**

Tranh cãi về việc đóng Head.

**Example:**

```typescript
bridge.commands.contest()
```

---

### **`HydraBridge.commands.recover(recoverTxId: string) → void`**

Phục hồi từ giao dịch cụ thể.

**Parameters:**

- `recoverTxId` (string): Hash giao dịch cần phục hồi

**Example:**

```typescript
bridge.commands.recover('tx_hash_here')
```

---

### **`HydraBridge.commands.newTx(cborHex: string, description: string, callback?: Function) → void`**

Gửi giao dịch mới tới Head.

**Parameters:**

- `cborHex` (string): Giao dịch dưới dạng CBOR hex
- `description` (string): Mô tả giao dịch
- `callback?` (Function): Callback khi gửi

**Example:**

```typescript
bridge.commands.newTx(signedTxCbor, 'Payment transaction', () =>
	console.log('Giao dịch đã gửi tới Head')
)
```

---

### **`HydraBridge.commands.decommit(payload: DecommitPayload) → Promise<DecommitResult>`**

Decommit UTxOs từ Head.

**Parameters:**

- `payload` (DecommitPayload): Dữ liệu decommit
  - `cborHex`: Transaction CBOR hex
  - `txId`: Transaction ID
  - `timeout?`: Timeout

**Example:**

```typescript
const result = await bridge.commands.decommit({
	cborHex: decommitTxCbor,
	txId: transactionId,
	timeout: 30000
})
console.log('Kết quả decommit:', result)
```

**Returns:** `Promise<DecommitResult>`

---

### **`HydraBridge.commands.initSync(retry?: number, interval?: number) → Promise<any>`**

Khởi tạo Head với logic retry.

**Parameters:**

- `retry?` (number): Số lần thử lại. Mặc định: 3
- `interval?` (number): Khoảng thời gian giữa các lần thử (ms). Mặc định: 20000

**Example:**

```typescript
const result = await bridge.commands.initSync(3, 20000)
console.log('Head đã khởi tạo:', result)
```

**Returns:** `Promise<any>`

---

## Connectors

### **`WebsocketConnector`**

Connector WebSocket cơ bản để kết nối trực tiếp tới Hydra Node.

**Constructor Parameters:**

- `websocketUrl` (string): WebSocket URL của Hydra Node
- `history?` (boolean): Bật nhận lịch sử node đầy đủ (mặc định: false)
- `noSnapshotUtxo?` (boolean): Vô hiệu hóa hiển thị snapshot UTxO (mặc định: false)
- `address?` (string): Lọc transaction outputs theo địa chỉ cụ thể

**Constructor:**

```typescript
import { WebsocketConnector } from '@hydra-sdk/bridge'

// Kết nối cơ bản
const connector = new WebsocketConnector({
	websocketUrl: 'ws://localhost:4001'
})

// Với các tùy chọn nâng cao
const connector = new WebsocketConnector({
	websocketUrl: 'ws://localhost:4001',
	history: true,
	noSnapshotUtxo: false,
	address: 'addr_test1...'
})

// Kết nối Hydra Hub với API key trong URL
const connector = new WebsocketConnector({
	websocketUrl: 'wss://hydra-hub.example.com?X-Api-Key=your_api_key_here'
})

// Kết hợp API key với các tùy chọn khác
const connector = new WebsocketConnector({
	websocketUrl: 'wss://hydra-hub.example.com?X-Api-Key=your_api_key_here',
	history: true,
	address: 'addr_test1...'
})

const bridge = new HydraBridge({
	connector: connector,
	verbose: true
})
```

---

### **`HexcoreConnector`**

Connector nâng cao cho Hexcore API integration với Socket.IO authentication.

**Constructor:**

```typescript
import { HexcoreConnector } from '@hydra-sdk/bridge'

const connector = new HexcoreConnector({
	socketIoUrl: 'wss://api.hexcore.io/hydra',
	socketIoOptions: {
		auth: {
			token: 'your_jwt_token'
		},
		transports: ['websocket'],
		timeout: 20000
	},
	namespace: 'hydra'
})

const bridge = new HydraBridge({
	connector: connector,
	verbose: true
})

await bridge.connect()
```

---

## Event Handling

Bridge cung cấp xử lý sự kiện toàn diện thông qua thuộc tính `events`:

### **Connection Events**

```typescript
// Kết nối thành lập
bridge.events.on('onConnected', () => {
	console.log('Đã kết nối tới Hydra Node')
})

// Kết nối bị mất
bridge.events.on('onDisconnected', () => {
	console.log('Đã ngắt kết nối khỏi Hydra Node')
})

// Lỗi kết nối
bridge.events.on('onConnectError', error => {
	console.error('Lỗi kết nối:', error)
})

// Lỗi chung
bridge.events.on('onError', error => {
	console.error('Lỗi Bridge:', error)
})
```

---

### **Head Lifecycle Events**

```typescript
bridge.events.on('onMessage', payload => {
	console.log('Nhận tin nhắn:', payload.tag, payload.timestamp)

	switch (payload.tag) {
		case 'Greetings':
			console.log('Lời chào từ Head:', {
				headStatus: payload.headStatus,
				headId: payload.hydraHeadId
			})
			break

		case 'HeadIsInitializing':
			console.log('Head đang khởi tạo với các bên:', payload.parties)
			break

		case 'HeadIsOpen':
			console.log('Head đã mở và sẵn sàng cho giao dịch!')
			break

		case 'HeadIsClosed':
			console.log('Head đã đóng, thực hiện fanout...')
			break

		case 'HeadIsFinalized':
			console.log('Vòng đời Head hoàn thành')
			break

		case 'TxValid':
			console.log('Giao dịch hợp lệ:', payload.transaction)
			break

		case 'TxInvalid':
			console.log('Giao dịch không hợp lệ:', {
				transaction: payload.transaction,
				error: payload.validationError
			})
			break

		case 'SnapshotConfirmed':
			console.log('Snapshot đã xác nhận')
			break

		case 'Committed':
			console.log('UTxO đã commit:', payload.utxo)
			break

		case 'DecommitApproved':
			console.log('Decommit đã phê duyệt:', payload.decommitTxId)
			break

		case 'DecommitFinalized':
			console.log('Decommit đã hoàn thành')
			break

		case 'CommandFailed':
			console.error('Lệnh thất bại:', payload.clientInput)
			break
	}
})
```

---

## Types and Interfaces

### HydraBridgeOptions

```typescript
interface HydraBridgeOptions {
	url?: string
	connector?: HydraBridgeConnector
	verbose?: boolean
	history?: boolean
	noSnapshotUtxo?: boolean
	address?: string
}
```

### HydraPayload

```typescript
interface HydraPayload {
	tag: HydraHeadTag
	timestamp: string
	seq?: number
	[key: string]: any
}
```

### HydraHeadStatus

```typescript
enum HydraHeadStatus {
	Idle = 'Idle',
	Initializing = 'Initializing',
	Open = 'Open',
	Closed = 'Closed',
	FanoutPossible = 'FanoutPossible',
	Final = 'Final'
}
```

### Protocol

```typescript
interface Protocol {
	min_fee_a: number
	min_fee_b: number
	max_tx_size: number
	key_deposit: string
	pool_deposit: string
	[key: string]: any
}
```

---

## Complete Examples

### Quản lý Head cơ bản

```typescript
import { HydraBridge } from '@hydra-sdk/bridge'

async function manageHydraHead() {
	const bridge = new HydraBridge({
		url: 'ws://localhost:4001',
		verbose: true
	})

	// Thiết lập handlers trước khi kết nối
	bridge.events.on('onConnected', () => {
		console.log('Đã kết nối! Đang khởi tạo Head...')
		bridge.commands.init()
	})

	bridge.events.on('onMessage', payload => {
		switch (payload.tag) {
			case 'HeadIsInitializing':
				console.log('Head đang khởi tạo với', payload.parties.length, 'bên')
				break
			case 'HeadIsOpen':
				console.log('Head đã mở và sẵn sàng cho giao dịch!')
				break
			case 'TxValid':
				console.log('Giao dịch đã xác nhận:', payload.transaction.txId)
				break
			case 'HeadIsClosed':
				console.log('Head đã đóng, thực hiện fanout...')
				bridge.commands.fanout()
				break
			case 'HeadIsFinalized':
				console.log('Vòng đời Head hoàn thành')
				bridge.disconnect()
				break
		}
	})

	// Kết nối để bắt đầu quy trình
	await bridge.connect()
}
```

### Xử lý giao dịch trong Hydra Head

```typescript
import { HydraBridge } from '@hydra-sdk/bridge'
import { TxBuilder } from '@hydra-sdk/transaction'
import { AppWallet } from '@hydra-sdk/core'

async function processHydraTransactions() {
	const bridge = new HydraBridge({
		url: 'ws://localhost:4001',
		verbose: true
	})

	const wallet = new AppWallet({
		networkId: 0,
		key: { type: 'mnemonic', words: AppWallet.brew() }
	})

	bridge.events.on('onConnected', async () => {
		// Lấy tham số giao thức
		const protocolParams = await bridge.getProtocolParameters()

		// Tạo transaction builder
		const txBuilder = new TxBuilder({
			isHydra: true,
			params: protocolParams
		})

		// Lấy tài khoản và UTxOs
		const account = wallet.getAccount(0, 0)
		const addressUtxos = await bridge.queryAddressUTxO(account.baseAddressBech32)

		if (addressUtxos.length === 0) {
			console.log('Không tìm thấy UTxOs cho địa chỉ')
			return
		}

		// Xây dựng và ký giao dịch
		const tx = await txBuilder
			.setInputs(addressUtxos)
			.txOut(account.baseAddressBech32, [
				{ unit: 'lovelace', quantity: '1000000' }
			])
			.changeAddress(account.baseAddressBech32)
			.complete()

		const signedTx = await wallet.signTx(tx.to_hex(), false, 0, 0)

		// Gửi tới Hydra Head
		const result = await bridge.submitTxSync(
			{
				type: 'Witnessed Tx ConwayEra',
				description: 'Ledger Cddl Format',
				cborHex: signedTx,
				txId: tx.id
			},
			{ timeout: 30000 }
		)

		console.log('Giao dịch thành công:', result)
	})

	await bridge.connect()
}
```

### Tích hợp Hexcore nâng cao

```typescript
import { HydraBridge, HexcoreConnector } from '@hydra-sdk/bridge'

async function connectToHexcore() {
	const connector = new HexcoreConnector({
		socketIoUrl: 'wss://api.hexcore.io/hydra',
		socketIoOptions: {
			auth: {
				token: 'your_jwt_token'
			},
			transports: ['websocket'],
			timeout: 20000
		},
		namespace: 'hydra'
	})

	const bridge = new HydraBridge({
		connector: connector,
		verbose: true
	})

	bridge.events.on('onConnected', () => {
		console.log('Đã xác thực và kết nối tới Hexcore')
	})

	bridge.events.on('onError', error => {
		console.error('Lỗi kết nối Hexcore:', error)
	})

	await bridge.connect()
}
```

### Tích hợp Hydra Hub nâng cao

```typescript
import { HydraBridge } from '@hydra-sdk/bridge'

async function connectWithApiKey() {
	// Truyền API key trực tiếp trong URL
	const bridge = new HydraBridge({
		url: 'wss://hydra-hub.example.com?X-Api-Key=your_secret_api_key',
		verbose: true
	})

	bridge.events.on('onConnected', () => {
		console.log('Đã xác thực và kết nối tới Hydra Hub')
	})

	bridge.events.on('onError', error => {
		console.error('Lỗi kết nối:', error)
	})

	await bridge.connect()
}
```

---

## Best Practices

1. **Event Setup**: Luôn thiết lập event handlers trước khi gọi `connect()`
2. **Error Handling**: Triển khai xử lý lỗi toàn diện và logic reconnection
3. **State Management**: Theo dõi trạng thái Head để đảm bảo các hoạt động hợp lệ
4. **Authentication**:
   - Với Hydra Hub: Truyền API key trong URL query params (`?X-Api-Key=your_key`)
   - Với Hexcore: Sử dụng JWT tokens trong Socket.IO auth options
5. **Resource Cleanup**: Luôn gọi `disconnect()` khi hoàn thành
6. **Transaction Validation**: Xác thực giao dịch trước khi gửi
7. **Timeout Configuration**: Đặt timeout phù hợp cho các hoạt động
8. **Logging**: Bật verbose mode để debug trong quá trình phát triển

---

## Related Documentation

- [Core API](/vi/2.api/1.core) - Wallet và utilities
- [Transaction Builder API](/vi/2.api/3.transaction) - Xây dựng giao dịch

---
title: '@hydra-sdk/bridge'
description: Tích hợp Hydra Layer 2 và quản lý Head
---

# @hydra-sdk/bridge — Tổng quan

Cung cấp đầy đủ tích hợp Hydra L2: kết nối, sự kiện, lệnh (commands), và xử lý giao dịch.

## Cài đặt

```bash
npm install @hydra-sdk/bridge
```

## Ví dụ ngắn

```ts
import { HydraBridge } from '@hydra-sdk/bridge'

const bridge = new HydraBridge({ url: 'ws://localhost:4001', verbose: true })
bridge.events.on('onConnected', () => bridge.commands.init())
bridge.connect()
```

> Xem thêm: [Tham khảo API](/vi/api/bridge/), [Ví dụ](/vi/examples/), [Hướng dẫn](/vi/guides/)

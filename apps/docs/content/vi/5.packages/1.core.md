---
title: '@hydra-sdk/core'
description: Chức năng ví Cardano và tích hợp mạng
---

# @hydra-sdk/core — Tổng quan

Cung cấp chức năng ví Cardano: quản lý HD wallet, ký giao dịch, tích hợp mạng, util serialization, key utilities.

## Cài đặt

```bash
npm install @hydra-sdk/core
```

## Cấu hình mạng

```ts
enum NETWORK_ID {
	TESTNET = 0,
	MAINNET = 1,
	PREPROD = 0
}

const wallet = new AppWallet({
	networkId: NETWORK_ID.MAINNET,
	key: { type: 'mnemonic', words: AppWallet.brew() }
})
```

## Ví dụ nhanh

```ts
import { AppWallet, NETWORK_ID } from '@hydra-sdk/core'

const wallet = new AppWallet({
	networkId: NETWORK_ID.PREPROD,
	key: { type: 'mnemonic', words: AppWallet.brew() }
})
const account = wallet.getAccount(0, 0)
console.log('Base:', account.baseAddressBech32)
```

> Xem thêm: [Tham khảo API](/vi/api/core/), [Ví dụ](/vi/examples/), [Hướng dẫn](/vi/guides/)
